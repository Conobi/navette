[project]
name = "navette-example-fetch"
version = "0.0.0"
description = "fetch — HTTP client example using navette"
requires-python = ">=3.11"
dependencies = [
    "navette",
    "boucle",
    "mojox>=0.2",
    "mojo-compiler==1.0.0b1",
]

[build-system]
requires = [
    "mojox-build>=0.3.0",
    "mojo-compiler==1.0.0b1",
    "mojo-compiler-mojo-libs==1.0.0b1",
    # mojo build of main.mojo needs cross-package imports resolved at build
    # time; PEP 517 build envs are isolated from runtime deps, so navette
    # and its Mojo deps are pinned here too.
    "navette",
    "boucle",
]
build-backend = "mojox_build"

[tool.mojox-build]
packages = []
binaries = [
    { source = "main.mojo", name = "fetch" },
]

[tool.uv]
prerelease = "allow"

[tool.uv.sources]
mojox-build = { path = "/home/donokami/Projets/perso/mojox/packages/mojox-build", editable = true }
navette     = { path = "../..", editable = true }
boucle      = { git = "ssh://git@github.com/Conobi/boucle.git", rev = "d0f8717c50a5aa40e6c513477fe02fee295dc76b" }
