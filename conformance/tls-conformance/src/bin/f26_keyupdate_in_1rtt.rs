//! F26 — KeyUpdate in 1-RTT.
//! GUARD-TAG: [TLS-KEYUPDATE-IN-1RTT]
//!
//! Phase α RED stub: exits 1 to feed the RED-log capture.
//! Phase β fills in adversarial 1-RTT injection + assertion.

fn main() {
    eprintln!("RED stub: F26 — TLS-conformance Phase α");
    std::process::exit(1);
}
