//! F09 — max_ack_delay >= 2^14.
//! GUARD-TAG: [QUIC-TP-MAX-ACK-DELAY-RANGE]
//!
//! Phase α RED stub: exits 1 to feed the AC-2 RED-log capture.
//! Phase β fills in adversarial TP construction + assertion.

fn main() {
    eprintln!("RED stub: F09 — TLS-conformance Phase α");
    std::process::exit(1);
}
