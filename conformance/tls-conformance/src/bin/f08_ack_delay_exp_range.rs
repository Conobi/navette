//! F08 — ack_delay_exponent > 20.
//! GUARD-TAG: [QUIC-TP-ACK-DELAY-EXP-RANGE]
//!
//! Phase α RED stub: exits 1 to feed the AC-2 RED-log capture.
//! Phase β fills in adversarial TP construction + assertion.

fn main() {
    eprintln!("RED stub: F08 — TLS-conformance Phase α");
    std::process::exit(1);
}
