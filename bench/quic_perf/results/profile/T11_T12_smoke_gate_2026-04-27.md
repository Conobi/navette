# T11/T12 Smoke Gate Results — 2026-04-27

Plan: `plans/2026-04-27-quic-queueing-tail-instrumentation.md`
Spec: `specs/2026-04-27-quic-queueing-tail-instrumentation.md`
Branch: `feat/quic-queueing-tail-instrumentation`

## T11 — Long-conn cell

`bench.sh mojo-net 1k long-conn tquic_client --iters 3`, both off-build and on-build, fresh Docker rebuild for each side.

| iter | off-build rps | on-build rps |
|---|---|---|
| 1 | 365.07 | 357.33 |
| 2 | 394.74 | 338.61 |
| 3 | 337.99 | 365.06 |
| **median** | **365.07** | **357.33** |

Drift: `(357.33 - 365.07) / 365.07 = -2.12%`. Threshold `≤10%`. **PASS.**

CPU%: off-build 5.1-5.5%, on-build 5.0-5.3%. No notable shift.

Failed handshakes: 40/iter on both sides — same baseline.

## T12 — Short-conn cell

`bench.sh mojo-net 1k short-conn tquic_client --iters 3`, both off-build and on-build, fresh Docker rebuild for each side. This cell exercises the saturating-handshake regime that drives Dict-op density on the hot path.

| iter | off-build rps | on-build rps |
|---|---|---|
| 1 | 0.26 | 0.32 |
| 2 | 0.52 | 0.26 |
| 3 | 0.35 | 0.64 |
| **median** | **0.35** | **0.32** |

Δ = on - off = -0.03 rps. Percentage drift = -8.6% of off-build, but the **absolute Δ = 0.03 rps is within the ±0.1 rps noise floor** documented in the plan T12 step 3 ("If both medians are within ±0.1 rps of each other, treat as PASS regardless of percentage; the percentage is meaningful only for non-trivial absolute values"). **PASS.**

CPU%: off-build 0.3%, on-build 0.2-0.3%. No notable shift.

Failed handshakes: 0-1 across all 6 iters (saturating-handshake regime — most attempts time out before reaching the server, hence the very low absolute rps).

## Verdict

Both smoke-gate cells PASS. The new instrumentation adds no measurable overhead at the granularity these cells can resolve. Per-flush-aggregate fallback (plan T11/T12 step 3) NOT triggered. Off-build flag restored to False. Ready to proceed with T13 operational capture under short-conn (note: capture will set PROFILE_ACCEPT=True again and rebuild Docker).

