# mojo-net — Project Context

**Last updated:** 2026-04-26
**Current phase:** spec-quic-accept-loop-instrumentation-plan-c-done — Plan C captured saturating-handshake data on branch `feat/quic-accept-loop-instrumentation`. **CONFIRMED: per-packet rustls FFI roundtrip on the handshake path is the cold-start bottleneck.** C1 baseline gate PASS (long-conn 405 rps median, short-conn 0.42 rps median — both reproduce calibrated 412/1 rps regime; B14's 5-handshake anomaly was a one-off). C3 short-conn capture: SM dominates by 64.5× (sm=129us, shim_ffi=127us — they overlap; almost all state-machine time IS FFI). C4 long-conn-c100 capture: drain (bench TX) dominates 4.7× (expected steady-state). Next hypothesis (high): Rust-side counters in `librustls-mojo/quic_hs.rs` to disambiguate FFI marshalling vs rustls work, prerequisite for choosing FFI-batching vs handshake-cache fix. Off-build flag restored to False.

## Why this exists

Pure-Mojo networking stack. The death-star deliverables are:
1. A fast HTTP/3 server
2. A feature-complete unified HTTP client (works over H1/H2/H3 with one API)

Single native dependency: rustls via a thin C FFI shim (`librustls-mojo`).

## Scope

- **Wire formats:** QUIC, HTTP/3, HTTP/2, HTTP/1.1
- **Servers:** H3 (primary goal), H2, H1
- **Clients:** Unified client with protocol selection via ALPN
- **TLS:** via librustls-mojo (rustls 0.23)
- **I/O:** sans-I/O at every protocol layer; `boucle` (separate library) used only in examples
- **Conformance:** test-vector + oracle cross-validation methodology (HC-1..HC-5, QC-1..QC-2)

## Non-goals

- Server push (deprecated in H2, dead in practice in H3)
- macOS/Windows I/O backends in v1 (Linux only via boucle)
- HTTP/0.9, SPDY, gQUIC (exception: HTTP/0.9 test infrastructure in `interop/` for QUIC Interop Runner participation)
- Connection migration in v1 of M3 (deferred enhancement)
- WebSocket support in v1 (revisit after M6)

## Key architectural decisions

- **Sans-I/O at every protocol layer.** Application code composes the protocol layer with an I/O loop (boucle in examples). No I/O imports inside `src/`. Exception: `boucle.stackful` (CoroHandle, CoroYielder) is allowed in `src/` because it is a control-flow mechanism with no I/O dependency. Only boucle's I/O primitives (`boucle.net.*`, `CompletionLoop`, `CompletionHandler`) are restricted to examples.
- **Strict-by-default parsing** with opt-in leniency via per-rule flags (24 flags in HTTP/1.1 ParserStrictness).
- **Conformance-driven development.** Each milestone has its own conformance suite with test vectors and oracle cross-validation before production code is written.
- **Test oracles:** h11 + httptools (HTTP/1.1), hyperframe (H2 frames), Python `hpack` (HPACK), hyper-h2 (HC-4 future), aioquic + quiche (H3/QUIC future).
- **Vector format:** JSON, consistent across all protocols.
- **Conformance vs production split:** `conformance/lib/` holds reference implementations used as test oracles; `src/` holds production code that is cross-validated against them.
- **librustls-mojo Wave model:** Wave 1 = TCP-TLS + AEAD primitives (done); Wave 2 = QUIC handshake lifecycle (spec written 2026-04-13, pending planning).
- **Wave 2 FFI convention:** All 15 Wave 2 functions return 0/-1 status with handles in `*out_handle` output params (differs from Wave 1's `rlsm_initial_keys` which returns the handle directly).
- **Wave 2 key transfer pattern:** `write_hs` buffers `KeyChange` in `QuicConnEntry.pending`; `take_keys` materializes it into Wave 1's `KEYS_TABLE`. `Secrets` (for key updates) stored separately in `next_secrets`. `alert_cache` field on `QuicConnEntry` implements clear-on-read alert semantics.
- **QUIC configs are process-lifetime singletons** — no config `_free` functions in Wave 2; callers must not create configs in unbounded loops.
- **QC-1 conformance vectors** (crypto + packet structure, ~35–45 vectors + aioquic oracle scripts) are a separate milestone between Wave 2 and M3 — mirrors HC-4 pattern.
- **M3 split into three sub-milestones:** M3a (codec ~1500-2200 LoC), M3b (connection core ~2200 LoC), M3c (data path ~2000 LoC). Each independently testable. Specs written sequentially (M3a first, M3b informed by M3a's actual types).
- **M3 builds both client + server simultaneously.** Connection state machine is 90% shared; only handshake initiation and HANDSHAKE_DONE direction differ. Enables loopback testing without external tools.
- **Hybrid state machine design:** Bitflags for Connection state (~22 orthogonal flags, TQUIC pattern), explicit enums for Stream state (6-8 sequential states per side, quinn pattern). Different complexity profiles warrant different approaches.
- **PTO in M3b, not M4.** Required for handshake correctness — without PTO, lost CRYPTO frames are never retransmitted and the 3x anti-amplification limit deadlocks the connection.
- **Full RESET_STREAM + STOP_SENDING in M3c.** quic-go v0.49.0 proved that retrofitting stream lifecycle onto stubs causes stalls, deadlocks, and memory leaks.
- **Dummy congestion controller for M3.** Tracks bytes_in_flight and time_sent (data M4 needs) but never reduces cwnd. Real CC (CUBIC/BBR) in M4.
- **Static flow control windows for M3:** 10 MiB connection, 1 MiB stream, 50% update threshold. Auto-tuning deferred to M4.
- **Security SHOULD/MAY = MUST.** PN skipping, ACK validation, amplification limits, PATH_RESPONSE caps — all mandatory. CVE analysis (50 CVEs across 14 stacks) showed 84% are remotely exploitable and most exploit optional security measures.
- **Basic stateless Retry in M3b.** Primary amplification defense. AEAD-encrypted tokens with server secret. NEW_TOKEN deferred to M4.
- **Test oracle strategy:** aioquic primary (pure Python, already integrated) + quiche as compliance reference (96-100% formal verification). Loopback tests (client↔server) need no oracle.
- **HTTP/3 is the canonical shape.** API design leads with H3 semantics; H2 and H1 are graceful projections of that shape onto older wire formats. Build order is H2 first (HC-4 → M5.5) for validation reasons, but the public API is designed against H3 semantics from M2.5 onwards.
- **H3CoroServer wraps QuicConnection directly** (same as H3HandlerServer), not H3Connection. Consistent constructor surface; hides QUIC details from callers. Trailer detection is implicit: second `HEADERS_RECEIVED` on same stream_id = trailers (trusted to H3Connection to prevent a third).
- **Handler execution model is callback-based** (γ hybrid: per-stream queue with `try_read` / `on_body_available`). Sync and async adapters deferred — Mojo's coroutine runtime is experimental and not pluggable to custom I/O sources today (verified via Modular docs + GitHub issues 2026-04-07).
- **Body ownership across handler callbacks: hybrid with explicit `detach()`.** Default is borrowed from runtime; handlers call `body.detach() -> DetachedBody` to take ownership for forwarding (reverse proxy) or wrapping (SSE). One-way; no resurrection.
- **Capability negotiation: runtime flags + standalone `H3StreamExtension` trait.** Not type-state generics (would create origin propagation through M6). Not trait inheritance with method override (fictional in Mojo). Standalone H3 trait avoids both pitfalls.
- **Session trait is compile-time monomorphic.** M6's connection pool composes via tagged enum or vtable indirection — decision deferred to M6 implementation time. The trait surface itself does not commit.
- **Client request ownership: owning, not borrowing.** `Session.submit(var req: Request)` consumes the request; `RequestHandle` owns derived state; no origin parameters propagating through `List[RequestHandle]`.
- **Default constants match hyper exactly.** 1 MiB stream window, 16 KiB max frame, 200 max concurrent streams, 20s keepalive. Subject to change after benchmarking; not stable API.
- **Mojo 0.26.2 conventions used in spec/code:** `def` for trait methods and ordinary methods; `fn __del__(deinit self)` for destructors; `def __init__(out self, *, deinit take: Self)` for move constructors; `comptime` constants instead of `@parameter`; integer constants for ALPN identifiers (no per-stream `String`).
- **M5 split into M5a + M5b.** M5a = H3 frame codec + QPACK static-only + Huffman + QC-2 conformance vectors (~1400–1800 LoC). M5b = H3 connection state machine + H3HandlerServer + H3Session + GOAWAY (~1300–1700 LoC). Mirrors M3a/M3b rhythm. M5b spec written after M5a is implemented (same pattern as M3a→M3b — M5b's types are informed by M5a's actual code).
- **QPACK static-only for M5.** Set `SETTINGS_QPACK_MAX_TABLE_CAPACITY = 0`. Create encoder/decoder QPACK streams per RFC requirement but keep them idle. Dynamic table deferred to M6. Huffman encoding included in M5a (pure codec function, no state).
- **QC-2 vectors folded into M5a** (not a standalone milestone). H3 frame + QPACK static vectors cross-validated against aioquic. Same oracle pattern as QC-1 (oracle script generates JSON; Mojo cross-validation reads and compares).
- **STREAMS_BLOCKED (RFC 9000 §4.6) as a micro-prereq before M5a.** ~180 LoC in connection.mojo + stream_map.mojo. Same dedup pattern as M4b's DATA_BLOCKED. Closes M4b required-later item.
- **M4 split into M4a + M4b.** M4a = CUBIC + pacer + persistent congestion + send gating. M4b = HyStart++ + delivery-rate + minmax + ECN + BLOCKED emission + PN skipping + FC auto-tuning. Same rhythm as M3's three-sub-milestone split. Decision driven by third-round reviewer scope concern: combined M4 exceeded the M3c ~2590 LoC single-plan ceiling.
- **CC pluggability via tagged-variant struct.** `CcController` combines QuicEvent's flat-tag pattern (`kind: UInt8` discriminant) with the nested-Copyable-struct pattern used elsewhere in M3 (e.g. `PacketNumberSpace` holding `EncryptionLevel`). Reserved `kind` values for future BBR/BBRv3. Variants declared `Copyable, Movable`; unused variant state wastes ~500 bytes per connection (negligible).
- **TQUIC is the primary QUIC reference implementation**, with quinn consulted on security-critical gaps where TQUIC is behind (notably PN skipping for CVE-2025-4820 optimistic-ACK defense — deferred to M4b but confirmed as MUST per security-posture decision line 51).
- **M6 two-layer client architecture.** `HttpClient` (sans-I/O) owns pool + dispatch + redirect/retry logic, testable with MockSession. `HttpCoroClient` wraps it with boucle.stackful for the user-facing ergonomic API. Matches the Server-side pattern (HandlerServer + CoroServer).
- **Session composition via tagged enum.** `SessionSlot` has `kind: UInt8` discriminant + `Optional[H1Session]` / `Optional[H2Session]` / `Optional[H3Session]`. Same pattern as `CcController` (M4a). Decision resolves the "tagged enum vs vtable" question deferred since M2.5a.
- **H3 never attempted speculatively.** UDP+QUIC path only when Alt-Svc explicitly advertises "h3". Default connections use TCP+TLS with H2/H1 ALPN negotiation.
- **Content-Encoding decoding via Rust FFI** (flate2 + brotli crates in librustls-mojo). Stateful streaming decoder (init/feed/finish lifecycle) to handle chunked responses incrementally.

## Active specs and plans

| Status | Spec | Notes |
|---|---|---|
| done | `specs/2026-04-25-quic-accept-loop-instrumentation.md` → Plan A shipped (on main); Plan B + Plan C done (on branch `feat/quic-accept-loop-instrumentation`). **Result: per-packet rustls FFI roundtrip dominates cold-start handshake path (sm/shim_ffi 127-129us, 64.5× next-leg in saturating-handshake regime).** Next: Rust-side counters in `librustls-mojo/quic_hs.rs`. | Plan A retro: `plans/2026-04-25-quic-accept-loop-instrumentation-plan-a-retrospective.md`; Plan B retro: `plans/2026-04-26-quic-accept-loop-instrumentation-plan-b-retrospective.md` | **QUIC accept-loop instrumentation pass — Plan A.** Cherry-picked to local main as 15 commits (`9255999..900067a`); branch `feat/quic-accept-loop-profile-a` deleted post-merge. Originally `78d42ea..9d6af58` on the feature branch. Standalone profile module: `src/quic/profile.mojo` (~430 LoC: comptime `PROFILE_ACCEPT` flag, sans-I/O `monotonic_us` via `InlineArray[Int64, 2]` stack-buffer + `clock_gettime`, `AcceptProfile` struct with 20 fields, 7 record_* methods, 24-bucket per-pkt histogram + linear-interp percentile estimator, exact-percentile helper for handshake latency vector, residual computation with underflow clamping, `report_text` + `report_json` formatters with comma-thousands + duration-us + percentage helpers — comma formatter byte-scans via `.as_bytes()` per Mojo 0.26.2 String-indexing constraint). 18 unit tests pass on both off-build and on-build. `test_quic_profile` registered in `scripts/run_tests.sh`. Conformance suite still 36/36. Final cross-cutting review CLEAN. Plan B (insertion + bench wiring + validation against full bench-mvp matrix) is deferred. **QUIC accept-loop instrumentation pass.** Hypothesis-data-gathering pass after the falsified pacer-bypass run. Goal: distinguish three suspects for the 412 req/s cold-start floor — fan-out / serialization on CQ wakeups, per-packet wall-clock cost in `feed_datagram_from_buffer`, and decomposition into rustls-shim FFI / AEAD / frame-parse / state-machine / drain / residual. Comptime-gated (`PROFILE_ACCEPT` flag); zero hot-path overhead off-build. New `src/quic/profile.mojo` module with 24-bucket histogram + exact handshake-latency vector, 4 insertion points + 1 deliberate non-insertion in `src/quic/connection.mojo`, bench wiring + SIGINT-flush + JSON sidecar in `bench/h3_server.mojo`. Spec went through 2 review rounds (4 BLOCKING + 9 IMPORTANT round 1; 1 BLOCKING + 6 IMPORTANT round 2; all addressed). Plan-split adopted: Plan A (~150 LoC, profile module + tests) → Plan B (~170 LoC, insertion + bench wiring + validation against full bench-mvp matrix). |
| in-progress | `specs/2026-04-25-mojo-net-python-bindings.md` → `plans/2026-04-25-mojo-net-python-plan-a.md` | **Python bindings Plan A — sync `mojo_net.Client`.** 29 TDD tasks across 9 phases: skeleton → 12 type bindings (Method/StatusCode/Headers/URL/BodyFrame/RequestBody/RecvBody/SendBody/Request/Response/RequestHandle/StreamError) → exception tree → DNS+TCP+TLS transport → H1/H2 sessions over loopback → `Client` facade → streaming/redirects → curated HTTPX-test subset → ≥ 2.0× httpx benchmark gate. Plans B (async `AsyncClient`), C1 (server core sync), C2 (server async + middleware) follow. Spec produced from 6-topic research investigation (`research/2026-04-25-python-interop-RESEARCH_PLAN.md`); all topics closed green. Independent spec review consumed and incorporated. |
| shipped | `specs/2026-04-07-m25-unified-http-api.md` → `plans/2026-04-07-m25a-unified-http-api.md` | M2.5a (HC-4 unblocker) — merged to main as `c93aaf9` and pushed to origin 2026-04-07. 30/30 src + 27/27 conformance + reverse-proxy e2e all green. Spec reconciled post-merge with inline `# M2.5a:` notes describing the five Mojo-0.26.2 forced deviations. |
| shipped | `specs/2026-04-07-m25-unified-http-api.md` → `plans/2026-04-07-m25b-helper-modules.md` | M2.5b (M6 unblocker) — merged to main as `3eb7b47` on 2026-04-09. 11 TDD commits + 1 post-review fix (`8f43e38`). 33/33 src + 27/27 conformance + reverse-proxy e2e all green. ~1217 LoC across 6 new files. Retrospective: `plans/2026-04-07-m25b-helper-modules-retrospective.md`. |
| done | `specs/2026-04-09-hc4-h2-connection-layer.md` → `plans/2026-04-09-hc4-phase0-trait-change.md` | HC-4 Phase 0 (prep commit: mut body + try_detach). 4 commits (`50b890e..f07a0f6`). 34/34 src + 27/27 conformance + e2e green. Final cross-cutting review: CLEAN. |
| done | `specs/2026-04-09-hc4-h2-connection-layer.md` → `plans/2026-04-09-hc4a-connection-core.md` | HC-4a connection core. 15 TDD commits (`64338e3..9955b95`). 24 unit + 3 cross-validation tests. 29/29 conformance + 34/34 src + e2e green. Retrospective: `plans/2026-04-09-hc4a-connection-core-retrospective.md`. Pushed to origin/main on 2026-04-10. |
| done | `specs/2026-04-09-hc4-h2-connection-layer.md` → `plans/2026-04-10-hc4b-stream-data-path.md` | HC-4b stream data path. 13 TDD tasks + 2 review fixes (`0e7108c..a1ff9d8`). 21 unit + 3 cross-validation tests. 31/31 conformance + 34/34 src + e2e green. Final cross-cutting review: CLEAN after 2 fix rounds. Pushed to origin/main on 2026-04-10. |
| done | `specs/2026-04-10-m55-h2-client-server.md` → `plans/2026-04-10-m55-h2-client-server.md` | M5.5 — HTTP/2 client/server + TLS/ALPN. 19 commits (`41b1e1d..90bf233`). H2HandlerServer (7 handler tests) + H2Session (7 session tests) + 3 e2e tests + 2 TLS ALPN tests. 39/39 src + 31/31 conformance + e2e green. Final cross-cutting review (opus): 3 important issues fixed (missing __del__, streaming body rejection, canonical response parser). |
| done | `specs/2026-04-13-m26-h2-coro-server.md` → `plans/2026-04-13-m26-h2-coro-server.md` | M2.6 — H2CoroServer + coroutine-based reverse proxy. 10 commits (`b11b815..18324fa`). H2CoroServer adapter (581 LoC) + 6 unit tests + proxy refactor (-231 lines net). 40/40 src + e2e green. Final review: 1 important issue fixed (concurrent backend handles). |
| done | `specs/2026-04-13-wave2-quic-handshake.md` → `plans/2026-04-13-wave2-quic-handshake.md` | Wave 2 — librustls QUIC handshake lifecycle. 15 FFI functions in new `quic_hs.rs` module. Config creation, connection lifecycle, write_hs/read_hs, key materialization into Wave 1 KEYS_TABLE, state queries, 0-RTT stubs. 10 TDD tasks. 39/39 tests green. |
| done | `specs/2026-04-13-qc1-quic-conformance.md` → `plans/2026-04-13-qc1-quic-conformance.md` | QC-1 — QUIC conformance. 6 commits (`a82d8a6..eb4548c`). Cat 1: rlsm_quic_client_config_with_ca FFI + 9 Mojo wrappers + key-pair consistency test (4 cross-checks). Cat 2: aioquic oracle + 20 packet header vectors + M3 stub. Cat 3: 4 AEAD + 2 full-packet vectors + test_aead + test_full_packet. 33/33 conformance + 39/39 cargo tests green. Final cross-cutting review: CLEAN. |
| done | `specs/2026-04-14-m3a-quic-codec.md` → `plans/2026-04-14-m3a-quic-codec.md` | M3a — QUIC codec & parsing. 11 commits (`eb4548c..1362f45`). 5 production modules in `src/quic/` (error, codec, frame, packet, trans_param). 20 frame types, 6 packet types, 17 transport params. 4 test files (66 test functions). 2 oracle scripts + 51 vectors. 44/44 src + 33/33 conformance tests green. Final cross-cutting review: CLEAN. |
| done | `specs/2026-04-14-m3b-quic-connection-core.md` → `plans/2026-04-14-m3b-quic-connection-core.md` | M3b — QUIC connection core. 22 commits (`1362f45..b701388`). 6 production modules in `src/quic/` (~2890 LoC): connection, pn_space, recovery, crypto_stream, packet_protect, retry. 21 QUIC FFI wrappers in `src/tls/lib.mojo`. 2 new Rust FFI functions. 5 test files, 7 integration tests (loopback handshake, close, idle timeout, loss, retry, coalescing, anti-amplification). 49/49 src + 33/33 conformance. Pushed to origin/main 2026-04-14. |
| done | `specs/2026-04-15-m4a-quic-cc-core.md` → `plans/2026-04-15-m4a-quic-cc-core.md` | M4a — CUBIC congestion control + TQUIC-style token-bucket pacer + persistent congestion detection. 15 commits (`b5b0cde..d9bbdcf`). 5 new cc/ modules (cc_trait, pacing, dummy, cubic, controller) + pn_space + recovery + connection.mojo wiring. 56/56 src + 33/33 conformance tests. Merged to main `c903917`. Retrospective: `plans/2026-04-15-m4a-quic-cc-core-retrospective.md`. |
| done | `specs/2026-04-17-m4c-transport-hygiene.md` → `plans/2026-04-17-m4c-transport-hygiene.md` | M4c — PN skipping (CVE-2025-4820 defense), SendBuf bare-FIN fix, FC doubling auto-tune. 7 commits (fc40d78..64e9d2a). |
| done | (no spec) → `plans/2026-04-17-streams-blocked-prereq.md` | Micro-task: emit STREAMS_BLOCKED when stream count limit is hit (RFC 9000 §4.6). 3 commits (`d9fd352..06841ae`). stream_map.mojo 4 new fields + open_stream flags; connection.mojo §9 emission + MAX_STREAMS reset; 2 integration tests. |
| done | `specs/2026-04-17-m5a-h3-codec.md` → `plans/2026-04-17-m5a-h3-codec.md` | M5a — H3 frame codec (DATA, HEADERS, SETTINGS) + QPACK static-only + Huffman + QC-2 conformance vectors. 15 commits (`3916044..4d34cdf`). 60/60 src + 35/35 conformance. §4.5.4 N/T bit swap + §4.5.6 combined-byte format fixed post-implementation via oracle cross-validation. Retrospective: `plans/2026-04-17-m5a-h3-codec-retrospective.md`. |
| done | `specs/2026-04-17-m5b-h3-connection.md` → `plans/2026-04-17-m5b-h3-connection.md` | M5b — H3 connection layer: H3Connection state machine + H3HandlerServer + H3Session + GOAWAY. QUIC loopback testing (no oracle). 62/62 src + 35/35 conformance. |
| done | `specs/2026-04-18-m5c-h3-coro-server.md` → `plans/2026-04-18-m5c-h3-coro-server.md` | M5c — H3CoroServer (coroutine-based H3 server adapter via boucle.stackful). Mirrors M2.6/H2CoroServer. ~380–450 LoC production + ~350–420 LoC tests. |
| done | `specs/2026-04-20-m6-unified-http-client.md` → `plans/2026-04-20-m6a-pool-dispatch.md` | M6a — HttpClient pool+dispatch+convenience. 7 commits (`3db2939..d8e3469`). URL parser, SessionSlot tagged enum, HttpClient with pool+dispatch+convenience. 65/65 src + 35/35 conformance tests. |
| done | `specs/2026-04-20-m6-unified-http-client.md` → `plans/2026-04-20-m6b-streaming-redirects-retries.md` | M6b — Streaming bodies + redirects + retries. 6 commits (`98f848f..3084ab9`). feed_body on Session trait, H1 chunked streaming, H2/H3 incremental DATA, redirect engine (301/302/307/308), retry helpers. 65/65 src + 35/35 conformance. |
| done | `specs/2026-04-20-m6-unified-http-client.md` → `plans/2026-04-20-m6c-coro-client-decoding.md` | M6c — HttpCoroClient + ContentDecoder + Alt-Svc. 5 commits (`9b266c5..2cf2db0`). gzip/brotli Rust FFI, Mojo ContentDecoder, HttpCoroClient adapter, Alt-Svc integration. 67/67 src + 35/35 conformance. |
| done | `specs/2026-04-21-interop-runner.md` → `plans/2026-04-21-interop-runner-a-foundation.md` | Interop Runner Plan A: SSLKEYLOGFILE in 5 config builders + interop/file_io (7 syscall functions) + interop/http09 (HTTP/0.9 client+server over QUIC). 5 commits (`983907c..c8dafc3`). |
| done | `specs/2026-04-21-interop-runner.md` → `plans/2026-04-21-interop-runner-b-integration.md` | Interop Runner Plan B: server + client + Dockerfile + local test. 7 commits (`0dfa8ad..61448ef`). UDP helpers, multi-connection server (handshake+transfer), client with fetch+save, Dockerfile, test script. |
| done | `specs/2026-04-17-m4b-cc-hardening.md` → `plans/2026-04-17-m4b-cc-hardening.md` | M4b — HyStart++ (RFC 9406) + MinMax RTT filter, ECN 3-stage path validation (RFC 9000 §13.4 + RFC 9002 §7.9), BLOCKED emission. 12 commits (`2a795bc..55ccd86`). 58/58 src + 33/33 conformance. Merged to main `5b54500` 2026-04-17. Retrospective: `plans/2026-04-17-m4b-cc-hardening-retrospective.md`. |
| done | (prerequisite for M4a) → `plans/2026-04-15-m3c-integration-coverage.md` | M3c integration-coverage prerequisite plan. 5 tasks, ~300 LoC new integration tests in `tests/test_quic_connection.mojo`. No production changes. Landed before M4a. |
| done | `specs/2026-04-21-quic-benchmark-suite.md` → `plans/2026-04-21-bench-server-a.md` | HttpArena benchmark Plan A: 10 commits (`e65124f..a2d72e4`). bench/handler.mojo (endpoint logic, BenchHandler, CoroBody fns), bench/h1_server.mojo (H1 :8080), bench/h2_server.mojo (H2 TLS :8443), bench/h3_server.mojo (H3 QUIC :8443 UDP), bench/run.sh. H1+H2 baseline2 verified via curl. Known issue: H1 static file serving returns 404 (H2 static works). |
| done | `specs/2026-04-21-quic-benchmark-suite.md` → `plans/2026-04-21-bench-server-b.md` | HttpArena benchmark Plan B: Dockerfile + meta.json + build.sh + HttpArena integration. 6 commits (`254ccad..34a111d`). First benchmark results: H1 2,825 req/s, H2 61,140 req/s, H3 0 req/s (ngtcp2 interop failure). |
| done | `specs/2026-04-22-bench-iouring-h3.md` → `plans/2026-04-22-bench-iouring-h3.md` | io_uring H3: CompletionLoop[H3UdpHandler] with 64 UdpRxSlot + dynamic UdpTxSlot + H3HandlerServer[BenchHandler]. 4 commits (`5bf1812..fcf751d`) + boucle commit `d311094` (submit_timeout). Integration: QUIC handshake + h3 ALPN + 2xx responses confirmed with h2load-h3/ngtcp2. Key fixes: raw socket syscalls (avoid OwnedHandle drop), peer-address connection keying, try/except around drain_and_send. |
| done | `specs/2026-04-22-bench-accept-multishot.md` → `plans/2026-04-22-bench-accept-multishot.md` | Accept multishot: IORING_ACCEPT_MULTISHOT for H1/H2. Boucle: Accept.ioprio() + submit_accept_multishot (d311094..aeb9b1f). mojo-net: H1+H2 conditional re-arm via IORING_CQE_F_MORE (fcf751d..3252880). |
| done | `specs/2026-04-22-bench-multi-process.md` → `plans/2026-04-22-bench-multi-process.md` | Multi-process launcher: fork/execv N workers with SO_REUSEPORT + sigtimedwait shutdown. 6 commits (f450c1b..a53a0f0). SO_REUSEPORT in all 3 servers, launcher with crash restart (max 3), SIGTERM→5s→SIGKILL. Dockerfile updated to use launcher as entrypoint. |
| done | `specs/2026-04-23-tls-ffi-quick-wins.md` → `plans/2026-04-23-tls-ffi-quick-wins.md` | TLS FFI quick wins. 3 commits (a96bb4e..f61d63a). Span pointer pass-through, pre-allocated drain buffers, cached handshake state, skip-locks in bench Dockerfile. Benchmark: H1 259K (+35%), H2 227K (+24%), H3 9.3K (~0%). |
| done | `specs/2026-04-23-quic-batch-pipeline.md` → `plans/2026-04-23-quic-zero-copy-ffi.md` | Phase 1: Zero-copy QUIC FFI + CID Dict. 9 commits (0c7640a..719d7b4). Zero-copy HP+AEAD in packet_protect, recv_from_buffer, _build_packet in-place, H3 wrappers, CID Dict, BENCH_PROTOCOL launcher filter. Benchmark (sequential, seccomp=unconfined): H1 291K, H2 180K, H3 3.8K. Docker crash was seccomp blocking io_uring_setup (not resource exhaustion). |
| done | `specs/2026-04-23-quic-batch-pipeline.md` → `plans/2026-04-24-quic-batch-ffi.md` | Phase 2: Batch Rust FFI. 6 commits (213eed8..fe8b3b3). 4 `rlsm_keys_batch_*` Rust functions + 4 Mojo FFI wrappers + 4 PacketProtect batch methods + integration test. Stage 1/2/3 recv split deferred to Phase 3. |
| in-progress | `specs/2026-04-23-quic-batch-pipeline.md` → `plans/2026-04-24-boucle-multishot-recvmsg.md` | Phase 3a: Boucle multishot recvmsg — RecvMsg ioprio/buf_group, ProvideBuffers op, BatchCompletionLoop. |
| in-progress | `specs/2026-04-23-quic-batch-pipeline.md` → `plans/2026-04-24-h3-batch-pipeline.md` | Phase 3b: H3 batch pipeline — provided buffer pool, multishot recvmsg on_complete, on_flush batch processing. Depends on Plan A. |
| done | `specs/2026-04-25-quic-pacer-bypass-handshake.md` → `plans/2026-04-25-quic-pacer-bypass-handshake.md` | **Hypothesis-first accept-loop optimization, pass 1 — FALSIFIED at single-cell gate, code shipped as RFC-compatible quality improvement.** Surgical bypass of the M4a pacer for non-`is_established()` connections at three sites in `src/quic/connection.mojo` (~15 LoC). Validation gate (median of 3 iters of `bench.sh mojo-net 1k long-conn tquic_client`): 415 rps vs threshold ≥ 4,000 — 1.01× speedup (within noise). Hypothesis falsified: pacer was not the cold-start handshake-throughput floor. CPU% remains ~5%, same idle-waiting symptom. **Shipped anyway** because the bypass is RFC-compatible (RFC 9002 §7's only normative MUST is "pace OR limit bursts to the initial cwnd"; retained anti-amp + cwnd satisfy the burst-limit clause). Reference-impl consensus is split: picoquic ships this exact design; quinn / TQUIC / ngtcp2 / quiche pace every encryption level. Commits: `911601e..ba3c254` on branch `fix/quic-pacer-bypass-handshake` → fast-forward to main. Open question recorded for next hypothesis: serial single-fiber `on_flush` in `bench/h3_server.mojo:523-600` (multi-fiber accept fan-out, severity: required-later, trigger: before any further QUIC perf work). |
| done | `specs/2026-04-25-quic-perf-bench-harness.md` → `plans/2026-04-25-quic-perf-bench-harness.md` | TQUIC-style perf bench harness for H3. 18 commits total: 15-task implementation (`0de376c..cde8b0d` + `9fe26cd`) shipped with three latent bugs that surfaced during post-implementation validation: pidstat sampling the wrong PID (`a0c6614` swaps to `docker stats`), tquic_client `--total-requests-per-thread` defaulting to 1 (`062804a` adds `0` for unlimited), and `/static/<file>` URL routing mismatch between mojo-net's BenchHandler and the bench scripts (`062804a` realigns URLs and remounts tquic_server payloads at `/data/static`). Calibrated REFERENCE.md (`b47b33c`): tquic_server saturates core 0 at 87K req/s (88% CPU); mojo-net measures 412/125 req/s long-conn and 1/11 req/s short-conn at <6% CPU — bottleneck is connection-establishment, not stream throughput. Lesson: the atelier flow's spec/plan/code-review cycle didn't catch bugs that only manifest at runtime; for measurement tools, code-review ≠ working software, so end-to-end smoke testing must come before the retrospective. |

## Constraints

- **Mojo:** 0.26.2 (see `~/.claude/projects/-home-donokami-Projets-perso-mojo-tquic/memory/project_mojo_syntax.md` for syntax notes)
- **TLS:** rustls 0.23 via librustls-mojo (Cargo.toml in `crates/librustls-mojo/`)
- **I/O:** boucle from `~/Projets/perso/boucle/` (CompletionLoop / ReadinessLoop)
- **Test runners:**
  - `bash conformance/scripts/run_tests.sh` (35/35)
  - `bash scripts/run_tests.sh` (60/60 src tests)
  - `bash scripts/test_reverse_proxy.sh` (TLS+io_uring e2e via Python backend)

## Milestone state (post-M2)

- **M2 — HTTP/1.1 + reverse proxy:** ✅ shipped, merged to main 2026-04-07
- **M2.5a — Unified HTTP API trait surface (HC-4 unblocker):** ✅ shipped, merged to main as `c93aaf9` and pushed to origin 2026-04-07
- **M2.5b — Helpers (priority, alt_svc, sse) (M6 unblocker):** ✅ shipped, merged to main as `3eb7b47` on 2026-04-09
- **HC-4 — HTTP/2 connection layer:** ✅ done — Phase 0 + HC-4a (connection core) + HC-4b (stream data path) all on main. 31/31 conformance tests. M5.5 unblocked.
- **M5.5 — HTTP/2 client/server + TLS/ALPN:** ✅ done — H2HandlerServer + H2Session + TLS ALPN. 39/39 src tests. 19 commits.
- **librustls Wave 2 — QUIC handshake lifecycle:** ✅ done — 15 FFI functions in `quic_hs.rs`. 39/39 tests green. Pushed to origin/main.
- **QC-1 — QUIC conformance:** ✅ done — 6 commits (`a82d8a6..eb4548c`). Cat 1 key-pair consistency + Cat 2 packet header vectors + Cat 3 AEAD completeness. 33/33 conformance + 39/39 cargo. Pushed to origin/main 2026-04-13.
- **M3a — QUIC codec & parsing:** ✅ done — 11 commits (`eb4548c..1362f45`). 5 production modules in `src/quic/` (error, codec, frame, packet, trans_param). 20 frame types, 6 packet types, 17 transport params. 66 test functions. 44/44 src + 33/33 conformance. Pushed to origin/main 2026-04-14.
- **M3b — QUIC connection core:** done — 22 commits (`1362f45..b701388`). Sans-I/O QuicConnection with TLS 1.3 handshake, 3 PN spaces, loss detection/PTO, anti-amplification, stateless Retry. 49/49 src + 33/33 conformance. Pushed to origin/main 2026-04-14.
- **M3c — QUIC data path + CID management:** spec (`specs/2026-04-14-m3c-quic-data-path.md`) → plan (`plans/2026-04-14-m3c-quic-data-path.md`), done. 13 commits (`b701388..9247747`). 4 new modules (flow_control, stream, stream_map, cid) + connection.mojo wiring. 53/53 src + 33/33 conformance tests passing. Retrospective: `plans/2026-04-14-m3c-quic-data-path-retrospective.md`.
- **M4a — QUIC congestion control core:** ✅ done — 15 commits (`b5b0cde..d9bbdcf`). `src/quic/cc/` subsystem (cc_trait, pacing, dummy, cubic, controller). CUBIC (RFC 9438) + TQUIC token-bucket pacer + persistent congestion (RFC 9002 §7.6.2). `_anti_amp_ok` / `_can_send` send-gate helpers. 56/56 src + 33/33 conformance. Merged to main `c903917` 2026-04-17.
- **M4b — QUIC CC hardening + interop:** ✅ done — 12 commits (`2a795bc..55ccd86`). HyStart++ (RFC 9406) + MinMax RTT filter, ECN 3-stage path validation (RFC 9000 §13.4 + RFC 9002 §7.9), BLOCKED/STREAM_DATA_BLOCKED emission. 58/58 src + 33/33 conformance. Merged to main `5b54500` 2026-04-17.
- **M4c — Transport hygiene:** ✅ done — 7 commits (fc40d78..64e9d2a). PN skip (Xorshift64, Application space, seeded from local_cid), SendBuf bare-FIN fix, FC window doubling (33% threshold, 8 MiB stream cap / 64 MiB conn cap). 58/58 src + 33/33 conformance. Pushed to origin/main 2026-04-17.
- **STREAMS_BLOCKED prereq:** ✅ done — 3 commits (`d9fd352..06841ae`). stream_map.mojo flags + connection.mojo emission + 2 integration tests. 58/58 src + 33/33 conformance.
- **M5a — H3 frame codec + QPACK + QC-2:** ✅ done — 15 commits (`3916044..4d34cdf`). `src/h3/` (error, frame, qpack). H3 frame codec (DATA/HEADERS/SETTINGS), 99-entry QPACK static table, Huffman encode/decode, QpackEncoder/Decoder (static-only). QC-2 conformance: 7 oracle-decode tests against pylsqpack vectors. Two RFC compliance bugs found and fixed via oracle cross-validation: §4.5.4 N/T bit swap (0x60→0x50) and §4.5.6 combined-byte format (H bit + 3-bit length prefix). 60/60 src + 35/35 conformance tests passing.
- **M5b — H3 connection + H3HandlerServer + H3Session:** ✅ done — commits `591c665..2ffc4e1`. H3Connection state machine (bootstrap, SETTINGS exchange, request demux, GOAWAY), H3HandlerServer (heap-allocated per-stream contexts, response draining), H3Session (Session trait, submit/run_one, received_goaway). 5 E2E tests (GET, POST, session, multi-request, GOAWAY). 62/62 src + 35/35 conformance tests passing.
- **M5c — H3CoroServer:** ✅ done — commits `d5281c6..dd8d46f`. H3CoroServer adapter (~317 LoC, `src/h3/h3_coro_server.mojo`) + 5 unit tests (POST body, trailers, RST_STREAM, GOAWAY). 63/63 src + 35/35 conformance tests passing.
- **M6 — Unified HTTP client:** spec written (`specs/2026-04-20-m6-unified-http-client.md`). Three phases: M6a (pool+dispatch+convenience), M6b (streaming bodies+redirects+retries), M6c (HttpCoroClient+decoding+Alt-Svc). M6a planning next.
- **M2.6 — H2CoroServer (async handlers via ucontext):** ✅ done — 10 commits (`b11b815..18324fa`). H2CoroServer adapter + proxy refactor. Supersedes blocked M2.6 (AsyncBody adapter). 40/40 src + e2e green.

## Open follow-ups (post-M2.5a)

- **M3c: SendBuf.on_ack ignores bare FIN-only frames** — ✅ fixed in M4c (`1a2de12`). Bare-FIN guard added inside `ack_len==0` branch.
- **M3c: BLOCKED frame emission** — ✅ implemented in M4b. `DATA_BLOCKED` + `STREAM_DATA_BLOCKED` emitted on FC exhaustion with `blocked_at` dedup guard.
- **M3c: Integration test coverage gaps.** Severity: required-later. Trigger: before M4 relies on behaviors that aren't tested end-to-end — specifically FC limit-violation error paths (FLOW_CONTROL_ERROR, FINAL_SIZE_ERROR), MAX_STREAM_DATA/MAX_DATA flow cycle, linear MAX_STREAMS growth via wire, CID retire→reissue exchange, and loss+retransmit of M3c frame types (RESET_STREAM, STOP_SENDING, MAX_*, NEW_CID).
- **M3c: Pre-commit mutation vs packet-build failure.** Severity: optional. Trigger: AEAD encryption failure post-handshake. `_build_frames_for_space` drains state (retire queue, mark_advertised, needs_* flags) before `_build_packet` runs — if AEAD raises, that state is lost and loss detection can't recover.
- **M3c: Spurious STREAM_READABLE events.** Severity: optional. Trigger: performance profiling. Each STREAM frame that touches the readable region fires STREAM_READABLE even if the app hasn't consumed anything yet.
- **M5b: H3HandlerServer._on_request swallows handler exceptions.** Severity: required-later. Trigger: when H3HandlerServer needs to respond with 4xx/5xx on handler error. If the handler raises in `on_request`, the stream context is not heap-allocated and not inserted into `_streams` (current behavior after fix), so the request silently drops. Same pattern as H2HandlerServer — should be addressed when proper error response dispatch is added.
- **M3c: app_frames_sent not cleaned on connection close.** Severity: optional. Trigger: memory growth under rapid connection churn. Entries for packets neither ACKed nor detected-lost linger until the connection is freed.
- **HC-4 design pass.** The HC-4a/4b/4c split was sketched before M2.5a's trait surface was concrete. Worth re-walking the spec against the now-shipped types — particularly `StreamHandler.on_request` taking `var body` (cross-callback streaming bodies need either `body.try_detach()` on a `mut` ref or splitting into `on_request_headers` + `on_request_body`), `Session.run_until` taking `Deque[UInt64]`, and `RequestBody` having no trailer slot.
- **Mojo native async re-spike.** Check Mojo changelog for `Waker` / `Executor` / `co.resume` exposure when 0.27 lands. If shipped, could replace ucontext-based coroutines with native ones (drop-in via CoroHandle API).
- **Reverse proxy main.mojo deeper refactor.** The example uses `H1Session` for the backend now (`68973f3`), but the client side still uses `ServerConnection` directly because `H1HandlerServer`'s synchronous `StreamHandler` model can't wait for an async backend response. A full `StreamHandler`-based refactor needs an async backend-response callback in the trait surface — natural HC-4 work.
- **Bench: Server crashes after first benchmark run.** ✅ Fixed 2026-04-21. Root causes: SQ exhaustion at 256 entries (increased to 4096), silent submit loss (per-submit try/except), use-after-free on close (deferred free when ops in-flight).
- **Bench: H3 QUIC interop failure with ngtcp2.** ✅ Fixed 2026-04-21. Root causes: RFC 9000 §7.2 violation (server used wrong DCID), IPv4-only UDP socket (converted to dual-stack).
- **Bench: Benchmark baseline (2026-04-22, single-threaded).** H1: 91,615 req/s, H2: 74,920 req/s, H3: 924 req/s.
- **Bench: Multi-process benchmark (2026-04-22, 8 workers, 8-core machine).** mojo-net: H1 192K req/s (wrk), H2 183K req/s (h2load), H3 9.5K req/s (h2load-h3). Reference same machine: nginx H1 491K / H2 560K (8 workers auto), hyper H1 465K / H2 663K (multi-threaded tokio). Throughput degrades ~10-15% across consecutive runs (suspected resource leak under sustained load).
- **Bench: TLS FFI quick wins (2026-04-23).** mojo-net: H1 259K (+35%), H2 227K (+24%), H3 9.3K (~0%). mojo-net vs nginx: H1 53%, H2 41%. mojo-net vs hyper: H1 56%, H2 34%.
- **Bench: Duplicate content-length header in H1 serializer.** ✅ Fixed `d732732`. Serializer unconditionally added content-length framing header even when handler already set it. Broke wrk (correctly rejects duplicate content-length as invalid HTTP/1.1). Fix: skip auto-add when `headers.has("content-length")`.
- **Bench: H1 static file serving returns 404.** Severity: optional. Trigger: `curl http://localhost:8080/static/index.html` returns 404. H2 static works fine with same handler code. Likely a Mojo move/copy issue in how BenchHandler passes the cache Dict through H1HandlerServer.

## Session history

- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan C executed directly via Bash on branch `feat/quic-accept-loop-instrumentation` (subagent-driven-development bypassed for operational tasks; prior B14 subagents abandoned mid-Docker-rebuild). 7 operational tasks; no code changes. **C1 GATE: PASS** — off-build long-conn 405 rps median (calibrated 412), short-conn 0.42 rps median (calibrated 1); both reproduce saturating-handshake regime; B14's 5-handshake anomaly was a one-off. **C3 short-conn sidecar: SM dominates by 64.5× (sm avg=129us, shim_ffi avg=127us — overlapping; almost all state-machine time is FFI roundtrip into librustls-mojo).** Only 17 successful handshakes in 30s (saturating-handshake confirmed); per_pkt p50=6 p90=101 p99=246; n=233 packets. **C4 long-conn-c100 sidecar: drain (bench TX) dominates 4.7×** (drain avg=52us; sm/shim_ffi only 8us each at this load — mostly steady-state stream serving with only 5 cold-starts; n=3571 packets). C5 synthesis: cold-start bottleneck is per-packet rustls FFI roundtrip on handshake CRYPTO path; 127us-per-packet × serialized single-fiber `_flush_impl` × ~100 attempts/30s = observed 99% timeout floor. C6 REFERENCE.md hypothesis-pass log entry committed; flag restored to False. **Next hypothesis (required-later, high):** Rust-side counters in `librustls-mojo/quic_hs.rs` (PREREQUISITE) to disambiguate FFI marshalling overhead vs rustls work itself; result chooses between (a) FFI batching for `quic_conn_read_hs/write_hs/take_keys` if marshalling dominates, or (b) handshake-cache for repeated client-Initials if rustls work dominates. Sidecars committed: `INSTRUMENTATION-20260426-203147-short-conn.json` + `INSTRUMENTATION-20260426-203304-long-conn-c100.json`. Branch HEAD post-commit. Phase: spec-quic-accept-loop-instrumentation-plan-c-done. Ready for finishing-a-development-branch.
- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan C written: `plans/2026-04-26-quic-accept-loop-instrumentation-plan-c.md`. 7 operational tasks; no code changes (uses Plan B's instrumentation as-is). C1 is a hard gate (off-build baseline reproducibility verification — abort if neither long-conn nor short-conn reproduces the calibrated 412/1 rps + 99% timeout pattern). C2-C4 capture two on-build sidecars: short-conn (primary) and long-conn-c100 (comparison; uses direct `docker run` to override harness's `--max-concurrent-conns 25` because `run-tquic-client.sh` hardcodes that). C5 applies ≥2× signal table to both sidecars. C6 appends REFERENCE.md hypothesis-pass log entry, restores flag, commits. C7 baseline test acceptance. Phase: spec-quic-accept-loop-instrumentation-plan-c-implementing. Ready for atelier:subagent-driven-development.
- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan C scoping (re-capture profile data under cold-start saturating-handshake load). Brainstorming-lite cycle: 4 decisions validated via AskUserQuestion. (1) C1 baseline-reproducibility gate: ON — run `bench.sh mojo-net 1k long-conn tquic_client --iters 3` AND short-conn off-build BEFORE any on-build profile capture; if neither reproduces the calibrated 412/1 rps + 99% timeout pattern, abort Plan C and fix the bench harness first. Plan B retro flagged this as the highest risk: B14's long-conn capture only saw 5 handshakes (steady-state) so the load pattern produced by `tquic_client 1k long-conn` may not exercise cold-start at all on this hardware/config combo. (2) Two on-build capture cells: `short-conn` (primary, retro-recommended as more reliable than long-conn at any concurrency) AND `long-conn --max-concurrent-conns 100` (comparison cell to disambiguate whether cold-start floor is short-conn-specific or concurrency-driven). (3) Single SIGINT capture per cell — matches B14, sufficient for ≥2× dominant-cost identification (signal dwarfs run-to-run noise); median-of-3 deferred unless results ambiguous. (4) Continue on renamed branch `feat/quic-accept-loop-instrumentation` (was `feat/quic-accept-loop-profile-b`) — plan-agnostic name covers Plan B + Plan C + future iterations; no FF-merge to main yet (Plan B + Plan C will land together). Plan C scope: ~5-7 operational tasks (no code changes; uses Plan B's instrumentation as-is). Watch-fors: short-conn handshake rate may make profile overhead more visible than B13's -0.40% on long-conn (5 conns); document drift if >10%. Phase: spec-quic-accept-loop-instrumentation-plan-c-planning. Ready for atelier:writing-plans.
- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan B execution complete via subagent-driven-development on branch `feat/quic-accept-loop-profile-b` off `900067a`. 14 tasks landed sequentially with per-task combined review (each ✅ CLEAN; B10 had 1 Important issue — MAP_FIXED → MAP_FIXED_NOREPLACE — fixed in-task; mid-execution Mojo 0.26.2 vs host 0.26.3.dev `thin`-qualifier compatibility issue fixed by dropping the redundant qualifier without affecting handler semantics). B13 single-cell smoke gate measured −0.40% drift on the load-bearing 1k long-conn cell (off-build 411.83 rps median vs on-build 413.46 rps median) — within run-to-run noise; the spec's ≤10% budget satisfied with ~25× headroom. B14 streamlined per user choice (option B — full bench-mvp matrix skipped); single-cell SIGINT-driven sidecar capture used instead. **Honest finding: the 30 s long-conn capture saw only 5 handshakes in steady-state (100% success), not the cold-start saturation pattern that motivated the spec. tquic_client opened 5 long-lived conns and pumped streams rather than reconnecting.** No in-recv leg dominates by ≥2× (frame_parse=11 / sm=8 / shim_ffi=7 / aead/hp/header_parse all sub-us); fan-out weighted-mean = 2.47 (well below ≥8 threshold); drain (bench TX) is largest at 31 us avg but is expected to dominate steady-state. Per-packet total p50=15 us p99=57 us (n=3,696). Idle 97.5% / busy 2.5% over 38 s wall. Handshake latency p50=1.3 ms but p99/max=29 ms — first-arriving conn paid Initial-key-derivation bleed-in cost (Plan B's profile_first_iter_done semantic harvesting it on iter 1) but n=5 too small to draw conclusions. Required-later (high): re-capture under short-conn scenario or `--max-concurrent-conns 100+` to exercise the cold-start saturating-handshake regime. Sidecar JSON committed: `bench/quic_perf/results/profile/INSTRUMENTATION-20260426-183256.json`. Branch left at HEAD (post-B14 commit) with profile flag restored to off-build (False). Ready for finishing-a-development-branch.
- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan B written: `plans/2026-04-26-quic-accept-loop-instrumentation-plan-b.md` (~17K tokens, 14 tasks). Re-read `src/quic/profile.mojo` HEAD to extract Plan A's actual signatures (verbatim — verifying the spec's pseudocode against integrated code per the M3a→M3b discipline; signatures match spec except: `record_pkt` is keyword-only with no `drain_us` param — `record_drain` is separate). Inspected current line numbers in `src/quic/connection.mojo` (struct line 279; server line 519; recv_from_buffer line 626; _drive_handshake line 1452; _on_handshake_complete line 1566) and `bench/h3_server.mojo` (H3UdpHandler line 323; _flush_impl line 529; _drain_and_send line 602; _handle_timeout line 654; main loop line 894). Each task spells out exact insertion points + complete Mojo code (no placeholders); per-task TDD structure (failing test → impl → pass → commit). Plan B introduces 4 always-present QuicConnection fields (3 from spec + `profile_first_iter_done` implementation flag), 4 insertion points + 1 deliberate non-insertion in connection.mojo, bench-side AcceptProfile field on H3UdpHandler with idle/busy/fan-out/drain/eviction-timeout instrumentation, SIGINT handler via `external_call["signal", ...]` flipping `Atomic[Int32]` flag, JSON sidecar via `external_call["time"/"gmtime_r"/"mkdir"/"fopen"/"fputs"/"fclose"]` with UTC `yyyymmdd-hhmmss` timestamp, README "Profile build" section with hand-edit recipe + SIGINT-latency caveat, single-cell smoke gate (B13) + full bench-mvp matrix (B14). Mojo 0.26.2 gotchas inlined at point-of-use: `InlineArray(fill=...)` keyword-only, `UnsafePointer(to=...)` not `.address_of`, `External_call` signature audits expected during build. Open questions documented: off-build asm spot-check (required-later, deferred — smoke is strictly stronger), `interop/udp.mojo` heap-alloc fix (required-later, trigger if smoke drift > 10%), `signalfd` io_uring integration (optional). Off-build asm-diff deliverable from spec marked DEFERRED in plan §Acceptance with explicit rationale (smoke drift gate is stronger). The DoD's 36/36 conformance suite gate retained (Plan A passed it; Plan B insertion is field-additive only at struct-level so should still pass). Phase: spec-quic-accept-loop-instrumentation-plan-b-implementing. Ready for atelier:subagent-driven-development.
- 2026-04-26 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Plan A pushed to origin (`b27ad86..900067a`, 15 commits). Plan B brainstorming session: validated 4 decisions via AskUserQuestion. (1) Add `monotonic_us` tight-loop microbench gate as Task B1 (1M-iter loop, ≤30ns/call gate) — pre-flight gate not in the spec, added per Plan A retrospective to abort cheaply if the timer budget is unreachable before the ~170 LoC of insertion work. (2) Defer the `interop/udp.mojo:266-276` heap-alloc fix until Plan B's on-build microbench shows drift attributable to it — keeps Plan B scope tight; the connection-side `monotonic_us` calls Plan B adds are ~10× more frequent than the bench-side ones, so any drift will be visible without preemptively touching `interop/`. (3) Stage validation as smoke (single-cell `bench.sh mojo-net 1k long-conn tquic_client --iters 3`) → full bench-mvp matrix (~50 min); two-task split of spec deliverables 2 + 3 with an early-abort if smoke fails. (4) New branch `feat/quic-accept-loop-profile-b` off `900067a` — mirrors Plan A pattern, isolates ~170 LoC + ~50 min of bench runs from main. Plan B task ordering: B1 microbench gate; B2-B6 connection.mojo (3 always-present fields with first-iter-bleed-in docstring at the field def, then 4 insertion points in dependency order: server constructor stamp → recv_from_buffer 5-phase → _drive_handshake FFI accumulator → _on_handshake_complete latency record); B7-B11 bench/h3_server.mojo (profile field + idle/busy/fan-out → profile_ptr threading + record_drain → eviction-site timeout count → SIGINT handler with atomic flag → JSON sidecar dir + UTC timestamp); B12 README "Profile build" section; B13 single-cell smoke; B14 full bench-mvp matrix + REFERENCE.md hypothesis-pass log + project-context update. No spec amendments — both new tasks (B1 microbench gate, B13 staged smoke) are plan-structure refinements, not new requirements; the spec stayed clean through 2 review rounds. Watch-fors recorded for plan author: first-iter-bleed-in docstring goes next to the field def (not just the spec); `AcceptProfile.Copyable+Movable` deep-copies 3 List[UInt64] — thread via `UnsafePointer[AcceptProfile, _]` only and add a docstring warning on the struct in `profile.mojo`; SIGINT-flush latency caveat must be prominent in the README. Phase: spec-quic-accept-loop-instrumentation-plan-b-planning. Ready for atelier:writing-plans.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/a6bba230-e8ba-4ff9-bed9-77951e354212.jsonl` (continued) — Plan A integration via finishing-a-development-branch. Retrospective written: `plans/2026-04-25-quic-accept-loop-instrumentation-plan-a-retrospective.md`. Branch divergence handled (same pattern as last session's pacer-bypass): main HEAD had advanced from `b27ad86` to `90ebdbb` via a parallel "Phase 2 coro-runtime plan" commit; the same content also appeared on the feature branch as `3ff65dc` (post-15-Plan-A-commits). Resolution: cherry-pick `b27ad86..9d6af58` (15 Plan A commits) onto main, skipping the already-on-main duplicate. Result on main: `9255999..900067a` (15 new commits). Feature branch deleted via `git branch -D` (cherry-pick produced new SHAs so `-d` refused). Tests still 18/18 PASS on integrated tip. Local main now ~26+ commits ahead of origin/main; push call deferred to user.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/a6bba230-e8ba-4ff9-bed9-77951e354212.jsonl` (continued) — QUIC accept-loop instrumentation Plan A executed via subagent-driven-development on branch `feat/quic-accept-loop-profile-a` (15 commits `78d42ea..9d6af58` off `b27ad86`). 14 TDD tasks landed sequentially with per-task combined review (each ✅ CLEAN). T1 skeleton (`comptime PROFILE_ACCEPT: Bool = False`, sans-I/O `monotonic_us`); T2 `AcceptProfile` 20-field struct + default __init__; T3-T8 the seven `record_*` methods (`record_idle`, `record_flush` with 8-bucket fan-out histogram, `record_pkt` with 7 leg sums + residual + 24-bucket dispatch + overflow + underflow clamp, `record_drain`, three handshake records); T9 `_exact_percentile` (insertion-sort + nearest-rank); T10 `_bucket_percentile` (linear interp inside containing bucket; spec-bug fix where the prescribed test value `5_000_000us` was correctly adjusted to `10_000_000us` because actual overflow boundary is 2^23 us); T11 `report_text` formatter with `_fmt_count` byte-scan formatter (per Mojo 0.26.2 String-indexing constraint) + `_fmt_pct` + `_fmt_duration_us` + `_fmt_leg`; T12 `report_json` formatter with `_json_leg` (verified parseable by python json.loads); T13 register `test_quic_profile` in `scripts/run_tests.sh`; T14 acceptance verification (off-build + on-build + conformance suite 36/36 unaffected). Final cross-cutting review found 1 Important issue: `monotonic_us` heap-allocated 16 bytes per call which would hurt Plan B's hot-path overhead budget; dispatched fix-it subagent who replaced it with `InlineArray[Int64, 2](fill=0)` stack buffer + `UnsafePointer(to=ts).bitcast[UInt8]()` (commit `9d6af58`). Re-review verdict: ✅ CLEAN. Mojo 0.26.2 gotchas encountered + resolved: `InlineArray` requires keyword `fill=` (positional `0` rejected); `UnsafePointer.address_of` not available in 0.26.2, use `UnsafePointer(to=...)`; `monotonic_us` migrated `def → fn` since stack-buffer version doesn't raise. 18 tests pass on both off-build and on-build; pre-existing `test_tls_connection` failure (missing `rlsm_client_config_new_insecure` rustls FFI symbol in shipped `.so`) is documented as out-of-scope. **Open question for Plan B (severity: required-later, trigger: before Plan B's on-build microbench):** Plan B will validate the spec's "≤10% on-build overhead" constraint by running `bench.sh mojo-net 1k long-conn tquic_client --iters 3` against off-build vs on-build; if drift exceeds 10%, reduce timer-call density.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/a6bba230-e8ba-4ff9-bed9-77951e354212.jsonl` (continued) — QUIC accept-loop instrumentation Plan A written: `plans/2026-04-25-quic-accept-loop-instrumentation-plan-a.md`. 14 TDD tasks producing the standalone profile module + unit tests, no `connection.mojo` edits, no bench wiring. Task breakdown: T1 module skeleton (`comptime PROFILE_ACCEPT`, `monotonic_us`); T2 `AcceptProfile` struct default `__init__`; T3 `record_idle`; T4 `record_flush` + 8-bucket fan-out histogram; T5 per-packet 24-bucket helper `_per_pkt_bucket`; T6 `record_pkt` (7-leg sums + residual + bucket assignment + overflow); T7 `record_drain`; T8 handshake records; T9 `_exact_percentile` (sort + nearest-rank); T10 `_bucket_percentile` (linear interp inside containing bucket); T11 `report_text`; T12 `report_json`; T13 register in `scripts/run_tests.sh`; T14 acceptance verification (off-build + on-build + conformance unaffected). Each task has failing test → minimal impl → pass → commit. Mojo 0.26.2 gotchas accounted for: no String indexing (use `.as_bytes()` + `chr(Int(...))` for `_fmt_count`); `from src.quic.profile import ...` works because `-I .` is on the run command. Plan B (insertion + bench wiring + validation) will be written after Plan A lands so it can target actual signatures.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/a6bba230-e8ba-4ff9-bed9-77951e354212.jsonl` — QUIC accept-loop instrumentation brainstorming (post-pacer-bypass). User asked "what should we do now to enhance QUIC perfs?" The previous pacer-bypass hypothesis was falsified at the single-cell gate (415 vs ≥4,000 rps threshold); the falsification log named the next suspect as the serial single-fiber `on_flush` loop in `bench/h3_server.mojo:523-600` plus per-packet shim-FFI cost. Argued for instrumentation before designing the fix to avoid a second falsified hypothesis. Validated 4 design decisions via AskUserQuestion (hook scope = bench + minimal src/quic; toggle = comptime flag; output = stderr + JSON sidecar; gate = full bench-mvp matrix). Spec written: `specs/2026-04-25-quic-accept-loop-instrumentation.md`. Round 1 review: 4 BLOCKING (convention conflict @parameter vs comptime; -D flag wiring underspecified; profile_ptr field breaks byte-identical claim; timing decomposition variable bindings inconsistent) + 9 IMPORTANT + 6 MINOR — all BLOCKING and load-bearing IMPORTANT addressed in revision (key changes: `comptime PROFILE_ACCEPT: Bool = False` declaration with hand-edit recipe, relaxed "byte-identical" to "zero hot-path codegen" with documented 8-byte struct drift exception, 5-phase disjoint timing table with explicit line bounds, dropped reservoir sampling in favor of 24-bucket histogram + exact handshake-latency vector, eviction-site timeout instrumentation alongside SIGINT sweep, monotonic_us defined locally in src/quic/profile.mojo to preserve sans-I/O boundary). Round 2 review: 1 new BLOCKING (DoD vs §connection.mojo insertion-count mismatch — "5 sites" vs "4 insertions" — fixed by renumbering as "4 insertions + 1 deliberate non-insertion") + 6 IMPORTANT (drain merge mechanism: dropped drain_us from record_pkt; added record_drain method on AcceptProfile; accumulator reset contradiction: explicit `profile_first_iter_done` flag with bleed-into-first-packet semantic; bucket histogram p99 unbounded error: extended to 24 buckets up to 4.2s + explicit `per_pkt_total_overflow` counter; phase-table residual undisclosed: added `residual_us = total - sum(legs)` as 6th line in report; SIGINT sweep undercount: added eviction-site instrumentation; missing `run_start_us` field: added to AcceptProfile) — all addressed. Plan-split adopted: Plan A (~150 LoC profile module + tests) → Plan B (~170 LoC insertion + bench wiring + full bench-mvp matrix validation). Open questions: Rust-side counters (required-later, trigger if shim_ffi.avg ≥ 2× next-largest leg); SIGINT signalfd integration with io_uring (optional). Phase: spec-quic-accept-loop-instrumentation-planning. Ready for atelier:writing-plans.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Hypothesis-first accept-loop brainstorming for the calibrated bench gap. Started by gating: ran the conformance suite + interop on the just-shipped REFERENCE.md HEAD (`b47b33c`) and discovered two stale H2 issues: (1) `test_h2_stream::test_send_data_window_exhaustion` asserts `send_data` raises on connection-window exhaustion — but commit `cc813cd` (2026-04-25 same-day) had deliberately rewritten `send_data` to fragment-and-queue (not raise) to fix the h2load `c100 m10 n10000` regression from 700/10000 → 10000/10000. The conformance test was stale, not a regression — and the new test (`tests/test_h2_send_window_exhaustion.mojo`) was orphaned, never registered in `conformance/scripts/run_tests.sh`. (2) `h2-abuse-padding-oracle` (severe) soft-fail in `test_h2_frame`: a DATA frame with `pad_length == frame_length - 1` (zero data, max padding) was being accepted; `src/h2/frame.mojo` used `>` instead of `>=` in the pad-length check. Fixed both via a worktree-isolated subagent + a follow-up subagent on a fresh `fix/h2-conformance-cleanup` branch (cherry-picked the padding-oracle commit, dropped the bad `connection.mojo` revert, rewrote the conformance test to assert queue-and-drain, moved orphan to `conformance/tests/`, registered in runner). Final: 36/36 conformance + interop green. Merged to main as fast-forward `b47b33c..2856437`. Lesson: cc813cd's retrospective explicitly noted "the orphan test is a one-line follow-up" but the conformance test that asserted the OLD contract was never updated — code review on cc813cd missed both halves of the cleanup. **Then started brainstorming pass 1 of the hypothesis-first accept-loop work.** Spawned an Explore agent to map mojo-net's accept→handshake hot path; pinpointed `Pacer.next_send_time` at `src/quic/connection.mojo:2660` as gating Initial+Handshake-space sends with cold-start pacing rate ≈ 60 KiB/s, ~20 ms back-pressure per subsequent datagram in a recvmsg burst. Hypothesis: pacing handshake-space packets was an unintended side-effect of M4a's universal `_can_send`; TQUIC/quinn/picoquic/ngtcp2 all pace 1-RTT only. Designed surgical bypass at three sites (`_can_send` gate, `_next_timeout` pacer-deadline branch, `process_send` post-send commit). Three decisions validated via AskUserQuestion (surgical only / single-cell-then-full validation / keep out-of-scope items separate). Spec written: `specs/2026-04-25-quic-pacer-bypass-handshake.md`. Independent spec review found 2 BLOCKING (post-send `pacer.on_sent` was actually drained for ALL spaces, not App-only — added Location 3 to gate under `if space_idx == 2:`; test setup was ambiguous about how to force pacer "would-block" — replaced with explicit field assignments) + 5 IMPORTANT (cite real CONN_ESTABLISHED write sites at 1041/1628/1639, acknowledge brief 1-RTT-keys-but-flag-unset window, bump validation to `--iters 3`, add handshake-padding regression test, quote actual pacer-test count from `tests/test_cc_pacing.mojo`). All resolved in revision. Plan written and executed via subagent-driven-development (4 commits on branch `fix/quic-pacer-bypass-handshake`: `911601e` planning, `db8cb6d` tests, `a5db31a` 3-location fix, `ba3c254` post-research doc correction). T1 (3 unit tests, T1 fails-on-main / 2+3 regression guards), T2 (3 connection.mojo edits + register test in `scripts/run_tests.sh`), T3 (conformance 36/36 + interop 3/3) all green. Combined reviews CLEAN per task. **T4 single-cell bench gate FALSIFIED the hypothesis:** median 415 rps vs ≥ 4,000 threshold (412 baseline; 1.01× — within noise); CPU% remains ~5%. The pacer was NOT the cold-start handshake floor. **Critical correction caught during integration:** my earlier "mirrors TQUIC/quinn/picoquic/ngtcp2" claim was an overclaim; verified against upstream sources, only picoquic ships this design (1-RTT pacing only); quinn/TQUIC/ngtcp2/quiche all pace every encryption level. Spec + connection.mojo docstring corrected in `ba3c254` to state honestly: "minority approach, RFC-compatible (RFC 9002 §7 only requires pace-OR-limit-bursts; retained anti-amp + cwnd satisfy the burst clause)". Code shipped as a code-quality improvement despite hypothesis falsification — strict improvement, no regressions, conformance + interop stay green; the bypass is correct even if not load-bearing on this bench. Branch fast-forward merged to main. **Next hypothesis (open question, required-later, trigger: before any further QUIC perf work):** serial single-fiber `on_flush` loop in `bench/h3_server.mojo:523-600` — multishot recvmsg delivers N Initials in one CQ wakeup but processes them strictly serially; with the pacer ruled out, this serial-fiber + per-packet FFI through the rustls global lock is the next prime suspect. T5 (full bench-mvp matrix) skipped — no point re-running with the same numbers.
- 2026-04-25 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` — QUIC perf bench harness (TQUIC-style) shipped with three latent runtime bugs that surfaced during post-implementation validation, *not* during the atelier review cycle. (1) `pidstat -p <docker-init>` was sampling the container init PID and missing all real server work, reading 0–1% CPU at 4,000 req/s — fixed by switching to `docker stats --no-stream` cgroup-level polling. (2) `tquic_client v1.0.0 --total-requests-per-thread` defaults to 1 (docs say "client exits on whichever limit hits first: total-requests OR duration"), so `--duration 30` was being preempted in <10 ms — fixed by adding `--total-requests-per-thread 0` (= unlimited). (3) `bench/handler.mojo` routes only `/static/<file>` to the cache; bench scripts requested `/<file>.bin` so 100% of mojo-net responses were 404 — fixed by realigning bench URLs to `/static/<file>` and remounting tquic_server payloads at `/data/static`. Three commits (`a0c6614`, `062804a`, `b47b33c`) — final REFERENCE.md is calibrated: tquic_server saturates core 0 at 87K req/s (88% CPU); mojo-net measures 412/125 long-conn and 1/11 short-conn at <6% CPU on core 0. Honest finding the harness now exposes: mojo-net is ~210× slower long-conn and ~2,500× slower short-conn than TQUIC on the same hardware, *not* CPU-bound — bottleneck is connection-establishment (only 3–10 of ~400 attempted handshakes succeed per 30 s under saturating load). Lesson for atelier: when the deliverable is a measurement tool, code-review (spec-vs-diff) ≠ working software; an end-to-end run of the tool against its reference oracle must come *before* the retrospective, not as part of post-merge cleanup. Local main is 25 commits ahead of origin/main; integration call (push vs keep-local) pending user.
- 2026-04-25 — Python-bindings Plan A written. `plans/2026-04-25-mojo-net-python-plan-a.md` — 29 TDD tasks across 9 phases. Per-task structure: failing test first, minimal Mojo or Python impl, build, pass, commit. Coverage: project skeleton (Tasks 1-2), 12 type bindings (Tasks 3-14: Method, StatusCode, Headers, URL, BodyFrame, RequestBody, RecvBody, SendBody, Request, Response, RequestHandle, StreamError), exception taxonomy (Task 15), DNS + TCP + TLS+ALPN + session factory + run_one (Tasks 16-21), `Client` facade + body draining + redirects + streaming (Tasks 22-25), 15-test curated HTTPX compat subset (Tasks 26-27), `≥ 2.0× httpx` benchmark gate (Task 28), retrospective + push (Task 29). New Mojo code lives under `bindings/python_client/`; rule: bindings/ may import from src/ only via public trait surface (StreamHandler, Session, RequestHandle, HttpClient, H{1,2,3}HandlerServer, H{2,3}CoroServer). Five small additions to src/ flagged at point-of-use: `Headers.len()/name_at()/value_at()/get_all()` accessors, `RequestBody._take_buffered`, `Response.__init__(other:)` copy ctor, `H1Session.from_fd` / `H2Session.from_fd` factories, `boucle.Socket.{connect_sync_v4,send_sync,recv_sync,close_raw}` sync wrappers. Ready for atelier:subagent-driven-development.
- 2026-04-25 — Python-bindings brainstorming session (`mojo-net-python-bindings`). User asked: how hard would it be to build HTTPX-equivalent + Starlette-equivalent on top of mojo-net, and what context is needed to answer credibly. Wrote `research/2026-04-25-python-interop-RESEARCH_PLAN.md` with 6 topics and dependency graph. Investigated all 6 in this session — each produced a `research/python-interop-*.md`. **All topics closed green, no blocking findings.** Critical unlock: Topic 4 confirmed ucontext stackful coroutines safely host CPython call-ins (3 callbacks × 2 yields × 1000 iterations zero-leak), opening the asyncio/Starlette path. Three Mojo `.so` spike artifacts under `/tmp/mojo_pyspike/` — module functions, move-only types, coroutine bridge all built and imported from CPython 3.14. Spec written: `specs/2026-04-25-mojo-net-python-bindings.md`. Independent spec review consumed (3 BLOCKING + 8 IMPORTANT + 5 MINOR findings); all blocking and most important resolved in revision. Spec splits into 4 plans: A (sync HTTPX `Client`, 4-6 wk), B (async `AsyncClient`, 3-4 wk), C1 (Server core sync handlers, 4-5 wk), C2 (async + middleware + lifespan + TestClient, 4-6 wk). Async driver model decision: uvloop-style asyncio backend (custom `MojoNetEventLoopPolicy`) — gives full ecosystem compat; ruled out "minimal-loop no-asyncio" and "drive-coro-without-loop" alternatives. Existing `bench/launcher` reused for multi-process workers. Hard dep on `modular>=26.2` (~235 MB user install). Linux-only Python wheels (matches mojo-net non-goal). Ready for plan A.
- 2026-04-07 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-tquic/629c84f4-5986-4f20-8a33-508efc270de7.jsonl` — M2.5 brainstorming. Decided HTTP/3-canonical shape, callback-only execution model, γ hybrid body with explicit `detach()`, standalone `H3StreamExtension` trait, compile-time monomorphic Session with M6 composition deferred, owning `RequestHandle`, hyper-matched defaults. Spec written, two rounds of independent review (1 blocking issue from round 1 fixed via `RequestBody` tagged union in round 2). Split into M2.5a + M2.5b. Ready for planning.
- 2026-04-07 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-tquic/7a5fc503-1831-4e4c-8509-d62960d67da7.jsonl` — M2.5a plan written. 22 TDD tasks, decision to add new `H1HandlerServer` / `H1Session` adapters next to existing `ServerConnection` / `ClientConnection` (rather than mutating the wire-format wrappers). DetachedBody push mechanism: typed-pointer via handler ownership, registry approach reserved as fallback. Open Mojo 0.26.2 questions (`def close(deinit self)`, copy ctors on Method/Version/Headers, Response body field) flagged at point-of-use for verification during implementation.
- 2026-04-07 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/d50abffa-5695-40f5-9018-fad00875cf17.jsonl` — M2.5a implemented on branch `m25a-unified-http-api` in worktree `.worktrees/m25a-unified-http-api`. All 22 tasks landed via TDD. 30/30 src tests + 27/27 conformance tests passing. Spec deviations documented in commits: (a) request trailers dropped in H1 parser since RequestBody is bytes-or-stream-only; (b) `Session.run_until` takes `Deque[UInt64]` of handle IDs instead of `List[RequestHandle]` because Mojo 0.26.2 stdlib collections require Copyable elements; (c) `StreamHandler.on_request` takes `var body: RecvBody` instead of `mut body` so handlers can `body^.detach()`; (d) `BodyFrame.error()` returns by value (not by ref) due to Optional-borrow origin issues; (e) `ResponseWriter` uses captured-state pattern with `_take_*` methods instead of the spec's `_send_headers_id: UInt64` registry. Fixes from Phase 1 spec/quality reviewers: SendBody.abort clears queued frames; RecvBody._set_end is idempotent; RecvBody._push drops post-terminal pushes; ResponseWriter._take_informational[_headers] expose 1xx data. Reverse proxy main.mojo refactor deferred — pre-existing boucle.net import errors on main are out of scope; new tests/test_reverse_proxy_refactor.mojo proves the trait surface supports the proxy pattern via MockSession backend. Research spike `research/mojo-async-executor.md` concluded M2.6 (AsyncBody) is blocked-on-Modular: Mojo 0.26.2 has no public waker API for plugging custom executors into the coroutine runtime.
- 2026-04-07 — same session, post-implementation cleanup: investigated the "boucle.net errors blocking the reverse_proxy refactor" follow-up and discovered the diagnosis was wrong — the example builds cleanly with the proper `-I "$HOME/Projets/perso/boucle"` include path, and the M2 e2e suite (`bash scripts/test_reverse_proxy.sh`) passes against the Phase 4 `Request.body → RequestBody` migration. Did a targeted refactor swapping the per-connection `ClientConnection` for `H1Session` (commit `68973f3`, +21 LoC) to validate the new Session trait against TLS+io_uring; client-side `ServerConnection` left in place because `H1HandlerServer`'s synchronous `StreamHandler` doesn't fit a proxy that needs to wait for an async backend response. Merged the m25a-unified-http-api branch onto main as merge commit `c93aaf9` (--no-ff to match project style) and pushed `1467bb0..c93aaf9` to origin/main. Removed the worktree and deleted the local branch. Reconciled `specs/2026-04-07-m25-unified-http-api.md` with inline `# M2.5a:` notes describing all five forced deviations + a "Reading this spec post-M2.5a" preamble so HC-4 readers see them up front.
- 2026-04-07 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/d50abffa-5695-40f5-9018-fad00875cf17.jsonl` — M2.5b plan written: `plans/2026-04-07-m25b-helper-modules.md`. 11 TDD tasks (Priority §7.1: 3 tasks; Alt-Svc §7.2: 4 tasks; SSE §7.3: 3 tasks; acceptance gate: 1 task). Two sketched deviations carried forward from M2.5a discipline: (a) flat `tests/test_*.mojo` layout instead of the §10.3 `tests/http/test_*.mojo` sketch; (b) `try_write_event` is a stateless free function instead of the spec's `EventStreamWriter` struct wrapping an `UnsafePointer[ResponseWriter]` (the spec's sketch contradicted its own "does not take ownership" doc and carried lifetime risk in Mojo 0.26.2). Open questions flagged inline at their call sites for implementation-time verification: `atol` availability, `Dict.pop` signature, string slice syntax, `KeyElement` trait composition for `Origin`, `WHATWG retry` parsing policy.
- 2026-04-07 — M2.5b implemented via subagent-driven-development on branch `m25b-helper-modules` in worktree `.worktrees/m25b-helper-modules`. All 11 tasks landed as 10 TDD commits (`2f1f1e2..486a366`) + 1 post-review fix commit `8f43e38`. Every task went through per-task spec + quality reviews; final full-range review (opus, cross-cutting) flagged an SSE livelock edge case (`is_end()` returning False forever if the body terminated mid-event) which was fixed by discarding the trailing partial-event buffer per WHATWG §9.2 semantics, with a new regression test `test_reader_partial_event_at_end_discarded`. 33/33 src + 27/27 conformance + reverse-proxy e2e all green. Mojo 0.26.2 forced deviations encountered during implementation: (a) `comptime X = N` without `: Int` annotation (matches existing `ALPN_*` pattern); (b) byte-scanning via `.as_bytes()` + `UInt8` constants + `chr(Int(b))` accumulation throughout all parsers because String `[i]` and `[start:end]` are unsupported; (c) `struct Origin(KeyElement)` instead of `(Copyable, Movable, Hashable, EqualityComparable)` — `KeyElement` is the composite Dict-key trait in `std.collections.dict`; `Hashable` lives under `hashlib.hash` and `EqualityComparable` does not exist (only `Equatable`); (d) `AltSvcCache.lookup/clear/clear_expired` marked `raises` because `Dict.__getitem__`/`pop` raise internally; (e) `AltSvcCache.clear` takes `origin: Origin` by borrow (not `var`) to avoid forcing callers to pre-copy; (f) `_split_top_level` takes `sep: UInt8` instead of `sep: String`; (g) `Optional[T]` direct-assign in copy ctors (Mojo 0.26.2 Optional has `.copied()` not `.copy()`, and Optional is `ImplicitlyCopyable`); (h) mid-file imports in `sse.mojo` for `ResponseWriter`/`WriteResult` matching the existing pattern in that file (`DetachedBody` is already imported mid-file). Worktree setup: symlinked `lib/` (librustls_mojo.so) and `conformance/vectors/hpack-stories/` (nested git repo fixtures) from the main repo checkout because worktrees don't inherit untracked files. Ready for merge to main and cleanup.
- 2026-04-09 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/9056decb-c699-4631-911d-55d872232f4b.jsonl` — HC-4 brainstorming + Phase 0 implementation. Brainstorming: researched 5 questions in parallel. Decided Approach A (mut body + try_detach). Spec written + reviewed. Phase 0: 4 commits (`50b890e..f07a0f6`). Per-task + final reviews CLEAN. 34/34 src + 27/27 conformance + e2e green.
- 2026-04-09 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/c5dc73e4-853c-4e68-8229-71315aebd2a9.jsonl` — HC-4a plan written + implemented. 15 TDD commits (`64338e3..9955b95`). H2Connection sans-I/O state machine (~870 LoC): preface, SETTINGS, PING, GOAWAY, stream table, CONTINUATION assembly, connection-level flow control. Oracle: Python h2 4.3.0. Tests: 24 unit + 3 cross-validation. Mojo deviations: Dict[Int, StreamState], make_stream_ended rename, .copy() workarounds.
- 2026-04-10 — same session — HC-4a pushed to origin/main. HC-4b plan written + implemented. 13 TDD tasks + 2 review fixes (`0e7108c..a1ff9d8`). Stream data path: HPACK decode/encode wiring, send_headers with CONTINUATION splitting, client response HEADERS, inbound DATA dispatch with recv window checks, send_data with fragmentation + send window checks, stream-level flow control (auto WINDOW_UPDATE), inbound trailers with END_STREAM validation, RST_STREAM handling (inbound + outbound stream close), SETTINGS INITIAL_WINDOW_SIZE window adjustment. Oracle extensions: h2_roundtrip, h2_stream_data_scenario. Tests: 21 unit + 3 cross-validation. 31/31 conformance + 34/34 src + e2e green. Final cross-cutting review (opus): 2 issues fixed (CONTINUATION trailer half-close transition, inbound DATA flow control check, trailer END_STREAM validation). Re-review: CLEAN.
- 2026-04-10 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/c5dc73e4-853c-4e68-8229-71315aebd2a9.jsonl` — M5.5 brainstorming + planning + implementation. Brainstorming: decided wrap pattern, full scope (server + client + TLS/ALPN), UnsafePointer per-stream state, drain-on-feed, separate ALPN setter FFI. Spec reviewed (opus): 3 blocking + 9 important resolved. Plan: 14 tasks across 5 phases. Implementation: 19 commits (`41b1e1d..90bf233`). H2HandlerServer wraps H2Connection with StreamHandler dispatch (7 tests). H2Session implements Session trait with concurrent handles (7 tests). 3 e2e client↔server tests. TLS ALPN: Rust FFI + Mojo wrappers + 2 negotiation tests. Final cross-cutting review (opus): 3 important issues fixed (missing __del__, streaming body rejection, canonical response parser). 39/39 src + 31/31 conformance + e2e green.
- 2026-04-13 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` — Wave 2 full cycle (brainstorm + plan + implement). Superseded LLVM coroutine research plan. Completed 5 research topics: rustls::quic handshake API, TQUIC architecture, RFC 9000 transport scope, conformance tooling (aioquic), QPACK+H3 scope. Decided: FFI-only Wave 2, include 0-RTT stubs, separate QC-1 milestone. Spec written + reviewed. Plan written: 10 TDD tasks. Implementation: 13 commits (20dd298..a4bd163). `quic_hs.rs` (~800 LoC): 15 FFI functions across §1 config, §2 connection lifecycle, §3 write_hs/read_hs, §4 key materialization, §5 state queries, §6 0-RTT stubs. Key adaptations: `AlertDescription as u8` → `u8::from(a)` (rustls 0.23); `conn.alert()` returns None for QUIC (CONNECTION_CLOSE instead of TLS Alert) → `unwrap_or(50)` synthetic fallback with documentation; `NoOpPacketKey` needed `confidentiality_limit`/`integrity_limit` trait methods. 39/39 tests green (11 Wave 2 + 28 Wave 1).
- 2026-04-13 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — QC-1 brainstorming. Decided: Full QC-1 scope (Cat 1 key consistency + Cat 2 packet header vectors + Cat 3 AEAD completeness), Wave 2 FFI complement-pair AEAD test (aioquic cross-validation deferred to post-M3 — requires M3 QUIC packet layer), aioquic oracle for Cat 2 packet header vectors, static vector file for Cat 2. Two blocking spec review issues resolved: (B1) redesigned Cat 1 to Wave 2 FFI self-consistency (not joint handshake) because Handshake/1-RTT keys are ECDHE-derived and two independent handshakes produce unrelated keys; (B2) dropped arbitrary-IV decrypt API (doesn't exist) in favor of handle-based `rlsm_keys_local_encrypt`/`rlsm_keys_remote_decrypt` at pn=0. Spec written: `specs/2026-04-13-qc1-quic-conformance.md`.
- 2026-04-13 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — QC-1 plan written: `plans/2026-04-13-qc1-quic-conformance.md`. 6 TDD tasks: Task 1 (Cat 3a AEAD vectors + test_aead), Task 2 (Cat 3b full-packet protect/unprotect + test_full_packet), Task 3 (Cat 1a: rlsm_quic_client_config_with_ca Rust FFI + 9 Mojo wrappers in conformance/lib/rustls.mojo), Task 4 (Cat 1b: test_cross_quic_hs_keys — cert gen via Python, handshake loop, 4 AEAD cross-checks), Task 5 (Cat 2a: oracle_quic_packet.py + packet_header.json), Task 6 (Cat 2b: test_cross_quic_packet_header stub + run_tests.sh update).
- 2026-04-13 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — QC-1 implemented via subagent-driven-development. 6 commits (`a82d8a6..eb4548c`). Tasks 1+3+5 dispatched in parallel (no file overlap), reviewed CLEAN. Tasks 2+4 dispatched in parallel, reviewed CLEAN. Task 6 dispatched, final cross-cutting review (opus): CLEAN. 33/33 conformance + 39/39 cargo tests green. Key implementation note: `cryptography.hazmat.primitives.ciphers.aead` is the correct import for AESGCM in cryptography 46.x (not `primitives.aead`).
- 2026-04-14 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M3a full cycle (research + brainstorm + plan + implement + finish). Deep research: 8 topics in parallel (RFC errata, amplification, flow control, PN spaces, loss detection, streams, CID, CVE analysis — 50 CVEs across 14 stacks). Design: 3 sub-milestones (M3a/M3b/M3c), both client+server, hybrid state machines, security SHOULD=MUST. M3a spec + independent review (0 blocking, 8 important resolved). Plan: 9 tasks. Implementation: 11 commits (`eb4548c..1362f45`) via subagent-driven-development. Phase 1: error+codec+oracles (2 parallel). Phase 2: frame+packet+trans_param (3 parallel). Phase 3: test files (3 parallel). Phase 4: integration. Per-phase reviews caught 3 bugs (short header fixed bit, ACK range underflow, ACK range truncation). Final cross-cutting review: CLEAN. 44/44 src + 33/33 conformance. Pre-existing test_tls_connection failure resolved (stale .so missing `insecure` feature). Pushed to origin/main. Retrospective: `plans/2026-04-14-m3a-quic-codec-retrospective.md`. Key lesson: Mojo MCP `validate` should be in every subagent prompt to catch known gotchas before compilation.
- 2026-04-14 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M3 brainstorming (deep research phase). 8 research topics completed in parallel via subagents: (1) RFC 9000 errata + implementation disagreements — 4 verified errata, quiche = gold standard at 96-100% formal verification; (2) amplification/address validation — QUICforge found 5/13 stacks violate 3x limit; (3) flow control correctness — 5 deadlock patterns cataloged; (4) PN space lifecycle — 128-bit dedup window, defensive space discard; (5) loss detection — PTO must be in M3b not M4; (6) stream state machine — RESET_STREAM+STOP_SENDING full in M3c; (7) CID management — retirement stuffing CVE-2024-22189 hit 11/17 stacks; (8) CVE analysis — 50 CVEs across 14 stacks, 84% remotely exploitable. M3 design: three sub-milestones (M3a codec, M3b connection, M3c data path), both client+server, hybrid state machines, security SHOULD=MUST. M3a spec written (`specs/2026-04-14-m3a-quic-codec.md`), independently reviewed (0 blocking, 8 important findings resolved). Research documents: `research/quic-rfc-errata-and-disagreements.md`, `research/quic-amplification-address-validation.md`, `research/quic-flow-control-correctness.md`, `research/quic-pn-spaces-key-transitions.md`, `research/quic-loss-detection-edge-cases.md`, `research/quic-stream-state-safety.md`, `research/quic-connection-id-management.md`, `research/quic-cve-pattern-analysis.md`.
- 2026-04-14 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M3b implementation + finishing. 22 commits (`1362f45..b701388`) via subagent-driven-development. Phase 1: Rust FFI (raw AES-GCM) + 21 Mojo FFI wrappers. Phase 2: pn_decode fix + crypto_stream + pn_space + recovery (4-way parallel). Phase 3: packet_protect + retry (2-way parallel). Phase 4: connection.mojo (1372 LoC orchestrator). Phase 5: 7 integration tests. Per-task reviews caught: CVE-2024-1765 window check, ACK range underflow, key handle leaks, bytes_in_flight underflow, stale pn_decode variable. Final cross-cutting review (opus) caught: server padding outside AEAD, CLOSING sends non-CC frames, missing PTO handler, wrong ack_delay exponent, idle timeout not using peer params. All fixed. 4 additional integration tests added post-review (loss, retry, coalescing, anti-amplification). Retrospective: `plans/2026-04-14-m3b-quic-connection-core-retrospective.md`. Pushed to origin/main.
- 2026-04-14 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M3b brainstorming + planning. Research already done (8 topics from M3 deep dive). Design: modular QuicConnection orchestrator delegating to pn_space, recovery, crypto_stream, packet_protect, retry. 4 decisions validated (modular arch, extend src/tls/lib.mojo, ACK-only deferred frames, standalone Retry). Spec written + independently reviewed (opus): 2 blocking + 13 important resolved (FFI inventory corrected, pn_decode fix clarified, initial PTO≠333ms, HP sample=16 bytes, bytes_in_flight added, time-threshold formula corrected, raw AES-GCM FFI needed for Retry). Plan: 11 tasks across 5 phases. Phase 1: Rust FFI + Mojo wrappers. Phase 2: pn_decode fix + crypto_stream + pn_space + recovery (4-way parallel). Phase 3: packet_protect + retry (2-way parallel). Phase 4: connection.mojo. Phase 5: integration tests + runner.
- 2026-04-13 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/bafe4277-bf40-4f56-8897-927600684a50.jsonl` — M2.6 full cycle (brainstorm + plan + implement + finish). Reviewed boucle stackful coroutines (ucontext FFI, commit `0877862`) — all tests pass, feedback given and addressed (mprotect raw syscall, __del__ assert, unchecked swapcontext comment, 3 new tests). Decided: new H2CoroServer adapter alongside H2HandlerServer (not modifying existing code), bare CoroBody function (not trait), handler-level coroutines only (I/O layer stays event-driven). Two blocking review issues resolved: (1) boucle.stackful allowed in src/ (control-flow, not I/O); (2) M2.6 label supersedes blocked AsyncBody adapter. Spec written + independently reviewed: 2 blocking + 5 important + 3 minor findings resolved. Spec: `specs/2026-04-13-m26-h2-coro-server.md`. Plan: `plans/2026-04-13-m26-h2-coro-server.md` — 10 tasks across 3 phases. Implementation: 10 commits (`b11b815..18324fa`) on main. H2CoroServer adapter (581 LoC, `src/h2/h2_coro_server.mojo`) + 6 unit tests (615 LoC) + proxy refactor (`examples/h2_reverse_proxy/main.mojo` 1165→1151 lines). Key deviation: `backend_handles: Dict[Int, UInt64]` replaces singular `Optional[RequestHandle]` to support concurrent backend submissions (review-caught bug). Key verification: mut method calls through UnsafePointer dereference work in Mojo 0.26.2. 40/40 src + e2e green. Retrospective: `plans/2026-04-13-m26-h2-coro-server-retrospective.md`.
- 2026-04-14 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M3c full cycle (brainstorm + plan + implement + review). Research already done. Decisions: modular split (4 new files), neqo-extended state machines (collapsed to 7 recv states in M3c), static flow control (50% threshold), full CID management. Spec written + independently reviewed (opus): 2 blocking + 11 important + 7 minor findings resolved before planning (FlowControl dual-counter split, fin_offset ownership, Stream struct definition, SendBuf ACK tracking, MAX_STREAMS linear formula). Plan: 7 tasks across 6 phases. Implementation: 13 commits (`b701388..HEAD`) via subagent-driven-development. Phase 1: FlowControl + CidManager (parallel). Phase 2: Stream module. Phase 3: StreamMap. Phase 4: Connection recv path. Phase 5: Connection send path + public API. Phase 6: Integration tests. Per-task reviews caught: FlowControl UInt64 underflow, CidManager replacement-on-retire, FIN retransmission on loss, urgency default, is_complete after read, RecvBuf read_offset handling (critical), MAX_STREAM_DATA spurious STREAM_WRITABLE, NEW_CID retransmit, round-robin rotation, SendBuf post-FIN guard, retire-queue cap on loss. Final cross-cutting review (opus): no ship-blockers, core invariants verified. 53/53 src + 33/33 conformance tests passing.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4b implementation + finishing. 12 commits (`2a795bc..55ccd86`) via subagent-driven-development. T0: MinMax windowed-min filter. T1: PN threading through on_packet_sent. T2: HyStart++ in cubic.mojo (SS/CSS phases, 8-sample RTT, `_hs_on_round_end`). T3: `_on_congestion_event` on Cubic + CcController. T4: ecn.mojo (EcnCounts, state constants). T5: pn_space ECN fields (recv_ecn, last_ack_ecn, ect0_in_flight, SentPacket.ecn_mark). T6: connection.mojo ECN wiring (ecn_mark() helper, outgoing ECT0, `_process_ecn_feedback`, 3-stage PROBING/CAPABLE/DISABLED). T7: BLOCKED emission (DATA_BLOCKED + STREAM_DATA_BLOCKED, blocked_at dedup, MAX_DATA/MAX_STREAM_DATA reset). T8: HyStart++ unit tests (8 + 4 MinMax). T9: ECN integration tests (7) + BLOCKED integration tests (4). Key fixes: `_hs_on_loss` moved after suppression guard; `_process_ecn_feedback` unconditional (not gated on has_ecn); `test_ecn_ce_triggers_congestion` rewrote from direct CC call to full end-to-end. Final cross-cutting review (opus): CLEAN. 58/58 src + 33/33 conformance. Merged to main `5b54500` 2026-04-17. Retrospective: `plans/2026-04-17-m4b-cc-hardening-retrospective.md`.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4c finish: retrospective written, pushed to origin/main `64e9d2a`. Phase → done.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — STREAMS_BLOCKED prereq plan written: `plans/2026-04-17-streams-blocked-prereq.md`. 3 tasks (failing tests → stream_map flags → connection.mojo emission + MAX_STREAMS reset). M5a plan written: `plans/2026-04-17-m5a-h3-codec.md`. 8 tasks (T0 error/init, T1 frame, T2 QPACK static table, T3 Huffman, T4 QpackEncoder, T5 QpackDecoder, T6 oracle, T7 cross-validation). Phase → implementing.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5 brainstorming. Decisions: M5 split into M5a (codec) + M5b (connection+handler+session); server+client both in M5b; STREAMS_BLOCKED as micro-prereq; QC-2 vectors folded into M5a; GOAWAY added to M5b scope; QPACK static-only (dynamic → M6), Huffman in M5a. M5a spec written + independently reviewed (2 blocking fixed: removed push frame constants; S=1/RIC=0 error added; 5 important resolved). Spec: `specs/2026-04-17-m5a-h3-codec.md`. Phase → planning.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4c implementation. 7 commits (fc40d78..64e9d2a) via subagent-driven-development. T0/T2/T4 test tasks in parallel, T1 (FC impl) + T5 (PN skip fields) in parallel, T3 (FIN fix) + T6 (seeding + integration) in parallel. Final cross-cutting review (opus): CLEAN. test_pn_oack_rejected replaced by test_pn_skip_next_in_valid_range (no direct process_ack hook on PacketNumberSpace).
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4c plan written: `plans/2026-04-17-m4c-transport-hygiene.md`. 6 tasks (T0 FC tests, T1 FC impl, T2 SendBuf test, T3 SendBuf fix, T4 PN-skip unit tests, T5 PN-skip impl, T6 seed + integration).
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4c brainstorming + spec. Three workstreams: PN skipping (CVE-2025-4820 defense, Xorshift64 seeded from local_cid, Application space only, first gap after 200–499 packets); SendBuf bare-FIN fix (add guard inside ack_len==0 branch, keep bottom check for non-zero path); FC doubling (should_update 50%→33%, update_limit doubles window to cap, max_window field, stream_map.mojo also updated). Spec reviewed (Plan subagent): 2 blocking fixed (method names should_update/update_limit, stream_map.mojo missing from change table); 3 important resolved (SendBuf fix clarified, PN-skip unit tests added, 0-RTT clause documented). Spec: `specs/2026-04-17-m4c-transport-hygiene.md`.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4b brainstorming + spec + plan. HyStart++ (RFC 9406) + MinMax RTT filter + ECN (3-stage path validation) + BLOCKED emission. Fixed 3 spec review issues (I1: split M4b/M4c, I3: CSS elif guard, I4: suppression window for CE). Plan: 10 tasks (T0 MinMax, T1 PN migration, T2 HyStart++ in cubic, T3 on_congestion_event, T4 ECN types, T5 pn_space ECN, T6 connection ECN wiring, T7 BLOCKED emission, T8 HyStart++ tests, T9 ECN+BLOCKED tests). Spec: `specs/2026-04-17-m4b-cc-hardening.md`. Plan: `plans/2026-04-17-m4b-cc-hardening.md`.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4a implementation + finishing. M3c-cov prereq plan (5 tasks, ~300 LoC integration tests). M4a implementation: 15 commits (`b5b0cde..d9bbdcf`) via subagent-driven-development. Phase 0: Mojo-API spikes (UInt128, nested-Copyable, UINT64_UNLIMITED, cc_trait.mojo naming). Phase 1: cc/ subsystem (cc_trait, pacing, dummy, cubic, controller). Phase 2: pn_space `last_ae_acked_time_sent` tracker + recovery CC/pacer integration. Phase 3: connection.mojo `_anti_amp_ok` extraction, `_can_send` helper, ACK flow rewire, persistent-congestion detection, `timeout(now)` pacer branch. Phase 4: integration tests + runner. Key review findings fixed: `cwnd_bytes → cwnd` rename (Task 4 reviewer), `_can_send` not wired into send path (T10+final reviewer — fixed `47167f4`), `CC_KIND_CUBIC/DUMMY` constants inverted (final reviewer — fixed `d9bbdcf`). 56/56 src + 33/33 conformance. Merged to main. Retrospective: `plans/2026-04-15-m4a-quic-cc-core-retrospective.md`.
- 2026-04-15 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M4 brainstorming. Decisions validated: CUBIC (not NewReno/BBR), Full hardening scope split into M4a/M4b (not single milestone, not 3-way split — M4a=~2590 LoC, M4b=~2000 LoC), M3c integration-coverage as short prerequisite plan before M4a (not inside M4a, no new milestone ID — extending M3c's done status), TQUIC-style token-bucket pacer (not quinn-complex, not no-pacer), include HyStart++ (user override of recommendation to defer — lands in M4b), defer ACK frequency extension, include quinn-style PN skipping (CVE-2025-4820 defense, deviation from TQUIC per security posture line 51). Three rounds of independent spec review (opus). Round 1: 5 blocking (dispatch pattern, persistent congestion RFC deviation, FC autotune missing write, ECN validation wrong, scope too large) → triggered split decision. Round 2: 3 blocking (max_ack_delay-in-persistent-congestion gated incorrectly on Data space only, 64-entry acked_time_ring undersized causing false positives, `_can_send`/`_anti_amp_ok` helpers claimed to exist but don't). Round 3: 3 blocking in `_anti_amp_ok` extraction (`is_client` vs `is_server` inversion, dropped `_addr_validated` guard, docstring/code contradiction) + 3 important (LoC mismatch header↔table, duplicate §9.6 heading, `timeout()` signature hedge). All fixed. Final spec: `specs/2026-04-15-m4a-quic-cc-core.md`. Saved feedback memory `feedback_mirror_tquic.md` with carve-out for security-critical gaps (prefer quinn over TQUIC when research/quic-cve-pattern-analysis.md flags unsafe TQUIC choices).
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5b plan written: `plans/2026-04-17-m5b-h3-connection.md`. 4 tasks (T0 H3Event+skeleton+unit tests, T1 full H3Connection impl+loopback, T2 H3HandlerServer+E2E, T3 H3Session+full E2E, T4 exports+run_tests.sh). Phase → implementing.
- 2026-04-18 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5c brainstorming. H3CoroServer mirrors H2CoroServer: wraps QuicConnection, CoroBody handlers, datagram transport API. Two decisions: wrap QuicConnection (not H3Connection), include trailer support (second HEADERS_RECEIVED = trailers). Spec written + independently reviewed: 2 blocking resolved (pop-before-free explicit code in cleanup helpers; ev.fin handling in _on_request for bodyless requests). Phase → planning.
- 2026-04-18 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5c plan written: `plans/2026-04-18-m5c-h3-coro-server.md`. 3 tasks (T0 h3_coro_server.mojo + simple GET test, T1 remaining 4 tests POST/trailers/RST/GOAWAY, T2 exports + run_tests.sh update). Phase → implementing.
- 2026-04-18 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5c implementation. 4 commits (`d5281c6..dd8d46f`) via subagent-driven-development. T0: H3CoroServer struct + CoroStreamCtx + _free_stream + simple GET test. T1: 4 remaining tests (POST body, trailers, RST_STREAM, GOAWAY) + 3 coroutine bodies. T2: H3CoroServer export + run_tests.sh update. All per-task and final cross-cutting reviews CLEAN. 63/63 src + 35/35 conformance tests passing. Phase → done.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5b implementation. 4 tasks via subagent-driven-development (`591c665..2ffc4e1`). T0: H3Event + _H3StreamBuf + H3Connection skeleton + 4 unit tests. T1: full H3Connection impl (feed_datagram, drain_datagrams, bootstrap 3 uni streams, SETTINGS, frame parsing, GOAWAY) + QUIC loopback test (SETTINGS_RECEIVED confirmed). T2: H3HandlerServer (heap-allocated _H3StreamCtx, _drain_responses, _maybe_cleanup) + test_h3_simple_get + test_h3_post_with_body. T3: H3Session (Session trait, submit, run_one, received_goaway flag) + test_h3_session_get + test_h3_multi_request + test_h3_goaway. T4: exports + run_tests.sh. Key fixes discovered during reviews: bidi rejection guard inverted (not peer_initiated → peer_initiated && !is_server); client bidi streams must be registered in _stream_bufs on open; _streams.pop before free in cleanup; dead _set_error in on_stream_reset; init_pointee_move roundtrip removed from run_one. Final cross-cutting review (opus): CLEAN. 62/62 src + 35/35 conformance.
- 2026-04-21 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — HttpArena benchmark brainstorming + planning. Researched HTTP benchmark tools (h2load, HttpArena, academic studies). Decided: HttpArena (TechEmpower successor) as benchmark suite, h2load/h2load-h3 as load generators, all 3 protocols (H1/H2/H3). Spec: `specs/2026-04-21-quic-benchmark-suite.md`. Two-plan split: Plan A (bench server + local validation), Plan B (Dockerfile + HttpArena integration). Key discovery: boucle needed `submit_recvmsg`/`submit_sendmsg` for UDP server in CompletionLoop — added to boucle. Plan A written: 7 tasks, 3 separate server binaries + shared handler. Phase → implementing.
- 2026-04-21 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` — QUIC interop runner brainstorming. Researched interop runner interface (research/quic-interop-runner-interface.md). Designed interop/ directory (isolated from src/). Key decisions: HTTP/0.9 test infra only (non-goal exception), SSLKEYLOGFILE via existing config builders + env var (no new FFI), multi-connection server with poll(2), raw syscalls for file I/O, DCID-based connection demux. Phase 1: handshake + transfer + http3 + retry. Two-plan split: Plan A (FFI + file_io + http09), Plan B (server + client + Dockerfile). Spec reviewed: 2 blocking (non-goal amendment, poll loop ambiguity) + 6 important resolved. Also: replaced XOR-fold reset token with real HMAC-SHA256 via Rust FFI (aws-lc-rs hmac::sign, commit 7c989e3). Phase → planning.
- 2026-04-17 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/22b55385-175c-45b6-b773-e60adb437edb.jsonl` (continued) — M5a finishing + gap closure. Final combined review (CLEAN after docstring fix). Retrospective written: `plans/2026-04-17-m5a-h3-codec-retrospective.md`. Post-retrospective gap closure: (1) rewrote `test_qpack_cross.mojo` from roundtrip-only to 7 real oracle-decode tests feeding pylsqpack hex bytes; (2) fixed §4.5.4 N/T bit swap (encoder 0x60→0x50, decoder mask 0x20→0x10); (3) fixed §4.5.6 encoder+decoder — encoder now combines H bit + 3-bit name length prefix into first byte (001 N H nnn); decoder now extracts H from bit3 and uses `qpack_decode_int(data, pos, 3)` for name length; (4) fixed static table entries 52, 57, 58 (missing spaces after semicolons); (5) pinned wire bytes in `test_encode/decode_literal_name_ref`; (6) added `test_huffman_encode_known_vector` (RFC 7541 C.4.1 "custom-key"). Commits: `9e69cb9` (fixes + tests) + `4d34cdf` (retrospective update). 60/60 src + 35/35 conformance. Phase → done.
- 2026-04-22 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Bench performance optimization brainstorming. Three independent specs written and reviewed: (1) io_uring H3 (`specs/2026-04-22-bench-iouring-h3.md`): replace poll(2)+recvfrom/sendto with CompletionLoop, H3HandlerServer[BenchHandler] callback handler, 64-slot RX pool, boucle prereq submit_timeout; (2) accept multishot (`specs/2026-04-22-bench-accept-multishot.md`): IORING_ACCEPT_MULTISHOT for H1/H2, critical finding: ioprio field not socket_flags; (3) multi-process (`specs/2026-04-22-bench-multi-process.md`): fork/execve launcher with SO_REUSEPORT. Implementation order: io_uring H3 → accept multishot → multi-process. Phase → spec-3-planning.
- 2026-04-22 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Docker benchmark: H1 192K (wrk), H2 183K (h2load), H3 9.5K (h2load-h3) with 8 workers. Reference: nginx H1 491K / H2 560K, hyper H1 465K / H2 663K. Found + fixed duplicate content-length bug in H1 serializer (`d732732`). Traced full data flow: 11+ copies per H1 request, 9-10 FFI crossings per H2 request.
- 2026-04-23 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — TLS FFI quick wins brainstorming. 4 surgical changes to src/tls/connection.mojo: (1) pass Span pointers directly to read_tls/write_plaintext, (2) pre-allocate drain buffers as TlsConnection fields, (3) cache is_handshaking state, (4) skip-locks in bench Dockerfile. Spec reviewed (0 blocking, 4 important resolved). Phase → planning.
- 2026-04-23 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — TLS FFI quick wins implemented. 3 commits (a96bb4e..f61d63a). Deviations: unsafe_mut_cast needed for FFI origin mismatch, _drain_write_tls writes directly to self._ciphertext_out (Mojo aliasing rules). Benchmark: H1 259K (+35%), H2 227K (+24%), H3 9.3K (~0%). Pushed to origin/main. Phase → done.
- 2026-04-23 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — QUIC zero-copy FFI implemented via subagent-driven-development. 8 commits (0c7640a..5cf7366). T0: unprotect_header_ptr + protect_header_ptr. T1: decrypt_payload_in_place + encrypt_payload_in_place + capacity guard. T2: recv_from_buffer in connection.mojo. T3: _build_packet zero-copy. T4: feed_datagram_from_buffer H3 wrappers + h3_server buffer pass. T5: CID Dict (swap-and-compact deviation from spec tombstone, accepted). Dict entry access fix. Final cross-cutting review (opus): CLEAN. H1 283K (+9% from 259K). H3 blocked by pre-existing Docker cert issue. Phase → reviewing.
- 2026-04-23 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — QUIC zero-copy FFI plan written: `plans/2026-04-23-quic-zero-copy-ffi.md`. 7 tasks (T0 HP zero-copy, T1 AEAD zero-copy, T2 recv_from_buffer, T3 _build_packet, T4 H3 wrappers + h3_server, T5 CID Dict, T6 benchmark). Phase 2+3 plans deferred.
- 2026-04-24 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — Phase 2 Batch FFI. 6 commits (213eed8..fe8b3b3). 4 Rust batch FFI functions (batch_header_unprotect, batch_decrypt, batch_header_protect, batch_encrypt) with single lock acquisition per batch + per-packet error reporting. Mojo wrappers on RustlsLibrary + batch methods on PacketProtect. Integration test: batch roundtrip via PacketProtect. Docker build verified. Final review: CLEAN (one spec wording divergence on nonce max-tracking — sequential update is safer, no fix needed). Phase → reviewing.
- 2026-04-24 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — QUIC zero-copy FFI finishing. Added BENCH_PROTOCOL env var to launcher for sequential protocol benchmarking (commit `719d7b4`). Root-caused Docker crash: seccomp blocks io_uring_setup (not resource exhaustion) — need `--security-opt seccomp=unconfined`. Benchmarked all 3 protocols sequentially: H1 291K (wrk), H2 180K (h2load), H3 3.8K (h2load-h3, 70% connection errors). Mojo String(from_utf8=) gotcha: do not append null terminator to List before constructing — Mojo handles it internally. Phase → done, branch kept as-is.
- 2026-04-23 — `~/.claude/projects/-home-donokami-Projets-perso-mojo-net/682f3d4b-5bc9-454e-afd2-f045e6edca1c.jsonl` (continued) — QUIC batch pipeline brainstorming. Analyzed underutilized HTTP/3+QUIC features research, dispatched 4 subagents (recv path, header+CID, frames+varint, crypto) to map the hot path. Found: 9 copies + 5 heap allocs per recv packet, 20-branch frame dispatch, linear CID scan, no batching anywhere. Designed simdjson-inspired 3-phase pipeline: Phase 1 (zero-copy FFI + CID Dict), Phase 2 (batch Rust FFI — 4 new functions), Phase 3 (multishot recvmsg + provided buffers + batch pipeline). Decisions: UnsafePointer recv API, multishot recvmsg (not CQ batching), per-packet error reporting, send path included in Phase 1. Spec reviewed (opus): 2 blocking (on_flush trait break → BatchCompletionHandler, recv_batch sans-I/O coupling → parallel arrays), 6 important resolved. Phase → planning.
